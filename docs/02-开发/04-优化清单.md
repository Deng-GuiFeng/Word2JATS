# 优化清单（持续推进,不停）

> 负责人要求:只要还有提高质量/名次的空间,就不停。按下面顺序滚动推进,每项都自测
> (pytest 72 项 + 10 样例三层确定性评测),改坏就回退,进展记在 `03-迭代日志.md`。
> 原则不变:只做能泛化的一类问题,不为单个样例过度工程;守内容守恒(不 OCR、不扩写、不编造)。

## 一、准确率:当前残差 backlog(R17 后,确定性档共 356 个缺陷)

> 数字与构成来自现行评测 `reports/eval/deterministic`(确定性档 `--llm off`,10 样例;
> 缺陷合计口径 = L0 error + L1 忠实 + L2 对位,本轮 L0 error 为 0、L1 计 73、L2 计 283;
> L0 warning/info 不计入合计、单列)。逐条明细可用
> `PYTHONPATH=scripts .venv/bin/python -m eval.run --llm off` 复现。
> 按处置方式分三类:**确定性可继续做 / LLM territory / 度量伪差或金标准人工判断(不宜追)**。

### A. LLM territory(已接兜底/闭环,确定性不硬啃)

- [ ] **参考文献难例**(L2 参考文献类 212 条,涉 9 个样例,最大项):Elsevier 书籍章节、会议录
      (如 "2015 37th Annual International Conference of the IEEE EMBC")、APA 体例、刊名含句点等
      ——确定性锚点(著录尾部 `年;卷(期):页`)在这些体例上对不齐,按设计整条落回 mixed-citation(零风险)。
      已交 `enrich/refs_llm.py` LLM 兜底(内容守恒护栏:字段须为原文子串,否则拒绝),
      +LLM 档实测进一步结构化(样例1 缺陷 93→43,见 `reports/eval/llm_agent`)。
      确定性侧只在"某个规整变体跨样例反复出现"时才补锚点,不为个别难例写死规则。
- [ ] **章节结构 / 后置声明长尾**(L2 章节 8 条、back声明 5 条):无样式子标题、裸声明句的结构歧义,
      归闭环修复器 A/C(`--agent` 档)。

### B. 确定性可继续做

- [ ] **表格多余属性 frame/rules**(L0 bare-attrs 警告 68 条,不计入 356):构建层仍给 `<table>` 加
      `frame="hsides" rules="groups"`,结构参考为裸表(td/th 上的 style/align 已在 R17 去掉,
      表级属性还没去)。直接删属性即可,预计一次清零。
- [ ] **`<aff>` 放置位置**(旧 #46):结构参考里 `<aff>` 是 `<article-meta>` 直属,我方仍在
      `<contrib-group>` 内(两者都 DTD 合法;现行评测单位类语义对齐 0 缺陷、不计放置位置)。
      如需与结构参考完全一致可迁移,属低风险确定性改动。
- [ ] **交叉引用长尾**(L2 交叉引用 19 条,除去下述度量伪差):个别引用写法识别边角
      (如 "Fig. 1a" 面板式、跨段区间)。
- [ ] **图与其余零星类目**(L2 图 5 条等):个别样例的图漏标/多标,逐条看 eval 明细。
- [ ] **L1 忠实长尾**(73 条):词级丢失/编造的零星残余(样例1 17 处、S03 21 处多为图面板号
      4a/4d 等)+ 样例2 的 11 项图片字节比对问题(md5 不匹配或缺文件,需对账外部化图与
      figures.zip 的来源),逐条看 eval 明细可继续收。(R17 后续把通讯块改为**忠实保留 docx 原文**
      后,样例2 通讯区块此前丢的 12 类词已归零。)
- [ ] B 档零碎项:作者贡献姓名缩写、章节编号/Title-Case(可开关、防过拟合)

### C. 度量伪差 / 金标准人工判断(不宜追,如实记录)

- **公式表示差异**(L2 公式 16 条,主要在样例3 的 15 条):R17 后续已把 **display 公式**里平衡的
  `<mo>(</mo>…<mo>)</mo>` 折成 `<mfenced open close>`(与金标准一致,`build/formulas.py` 的
  `_fold_fences`),样例3 公式缺陷 19→15。残余是语义键把 MathML 展开成文本串时的 near-miss
  (下标次序、上划线、原子切分,报告注明接近度 92–97%),数学内容一致,属度量伪差,不追。
- **制表符表列数**(L2 表类 18 条,涉 3 样例):金标准的列数与表头合并是**人工判断**
  (把对齐填充并列、合并双层表头等),确定性切 Tab 保证"不丢表、不丢字",但无法精确复刻人工取舍;
  不设"精确复刻"目标(设了就是过拟合)。
- **xref id 命名**(如 fn1 vs fn-1):金标准自身命名不一致,评测已按最近似匹配注明(接近度 96%),
  属度量伪差。
- **S03 图表引用**:金标准的编辑取舍是"不链接",我们忠实按源识别并链接 → 被计为多标;不追。

## 二、成本与速度(准确率到顶后压成本)

- [x] 并行榨干两卡:参考文献 LLM 兜底多批并发提交(表格已全走确定性构建,R17 删除了表格预热——预热成了空转)
- [x] 缓存覆盖:文本/视觉调用、CrossRef 全缓存;空响应不缓存(R14,避免冻结瞬时失败)
- [x] 闭环渲染 DPI 默认 **120**(实测 OCR 可读),可经 `--agent-dpi` / `ConvertOptions.agent_dpi` 配置(早期评测档曾试过 84,R15 已统一回 120,经过见迭代日志 R14/R15)
- [ ] 小模型替代:4B/0.8B 能接住的任务就用小的,省算力
- [ ] 把 35B 改成 1 卡 + 4B 1 卡,文本高频用小模型、难点用大模型

## 三、与出版领域相关的其他质量项

- [ ] JATS4R Schematron 校验(DTD 之外的出版级规则)
- [ ] XML→PDF 渲染做自检(复用基线 FOP 思路,肉眼/结构核对)
- [ ] 扩充 IMR 期刊表(覆盖更多刊,DOI 自动识别)
- [ ] 公式 alttext(LaTeX)、语义 MathML 增强
- [ ] 附件/补充材料处理(样例1 有 Supplementary Material.zip)
- [ ] 决赛 PPT 与演示

## 进度
逐项完成后在 `03-迭代日志.md` 记结果(数据 + 是否泛化 + 有无回退)。

---
## R11 决策(2026-06-26):精度迭代暂停 ——⚠️ 已被 R14/R15 推翻
新样例经三方交叉验证的伪标签确证、输出 0 差异;主样例对金标准 overall_score 0.86–1.0(因金标准含不可从输入还原的出版层编辑,以"忠实于源 docx"为真值,分数高即视为达标)。当时判断无失败信号、继续调即过拟合,故暂停精度迭代。
> **注:这个"暂停"判断在 R14/R15 经逐输出独立 subagent 据源审查后被推翻**——确有真缺陷(章节层级错位、核心公式漏抽、ORCID 张冠李戴等),精度工作已恢复,详见下文 R15 与迭代日志。
当时**保留**的非过拟合方向(待新样例或决赛阶段再评估):≥3 行表头、图片/制表符表的视觉重建质量、JATS4R Schematron 全量校验、决赛 PPT/演示打磨。

## R14 稳健性审计待办(2026-06-29,多代理审计确认,按优先级)

> 说明:以下及下方"处置对账"一节均为**面向开发的技术待办**,会直接出现代码内部符号(如 `_table_wrap_from_json`、`refs_llm`)、OOXML 原始标签(`w:sdt` 等)和 JATS 元素(`<thead>/<tbody>`、`table-wrap-foot`、`@id` 等);这些术语的通俗解释见 `../00-调研/02-数据与映射规格.md` 与 `../01-设计/02-智能循环架构.md`。每条结尾尽量点出"对输出的实际影响"。
本轮派多代理审计全管线,确认"相同输入 + 相同模型响应"下核心转换是**确定性**的;主要风险是"模型抖动时静默丢内容"。已修最高杠杆三项(见迭代日志 R14):①表格失败无损兜底 ②失败可观测 + 空响应不缓存 + 表数对账 ③参考文献整类丢失检测。以下为已确认、本轮**未做**的项(留作后续,均不影响默认路径的确定性):

- [ ] **公式转换失败兜底**:OMML→MathML 对个别畸形/不支持构件转换失败时,`disp/inline-formula` 会丢公式;建议回退为含 `<tex-math>`(取 OMML 文本)的合规元素而非丢弃。(注:此处指**能进入转换的**公式都能正常转,故低频;样例1 仍有一条核心公式丢失,但成因是该 OMML 根本**没进入抽取路径**——与本条的"转换失败"是两回事。)
- [x] **表格 JSON→XML 单元格保真**:R17 后确定性制表符表路径(`build_table_from_text_deterministic`)
  已按"列数=各行最大值"构建、不截断;`_table_wrap_from_json` 本身的截断只影响 LLM 表格路径,
  而该路径(看图/文本模型建表)已不在管线上,本条随之消解。
- [ ] **解析层 walker 完整性**:`docx_reader`/`runs` 对 `w:sdt`(内容控件)、`mc:AlternateContent`(图形回退)、单元格内嵌套表、`w:fldSimple` 等未识别结构会静默跳过;建议下钻这些容器并加解析日志。(样例未触发,属防御性。)
- [ ] **确定性专项(仅启用相应功能时才需)**:`--crossref` 冷缓存只缓存成功结果(避免瞬时失败被永久冻结)+ 收紧 top-1 采纳;`copyright-year` 在收/修/录日期全缺时**目前回退为 `today().year`**(见 `pipeline.py` 的 `default_year`),存在跨年漂移、尚未做到完全确定性——待办即把此回退改为"日期缺失则省略 `copyright-year`"。(不开 `--crossref` 时其余路径已确定性。)
- [ ] **图源数目对账**:图号超出可用图片数时,`<fig>` 会无 `<graphic>` 且无诊断;建议告警并记入对账。

## 上一版清单(R16 口径 `#NNN` 缺陷编号)在 R17 后的处置
R16 时列过一批跨样例系统性根因(编号 `#NNN`)。R17 内容守恒重构(见 `03-迭代日志.md` R17)之后逐项对账如下,
避免旧编号悬空;**现行 backlog 以上文"一、准确率"三分类为准**:

- **#137 参考文献 mixed-citation 兜底(原最大根因)→ 已修**:搜索式确定性切分结构化 492/522 条
  (字段匹配 93%),级联的作者拆分/刊名/卷页随之收回;残余难例即上文 A 类第一条。
- **#47 通讯作者归属/邮箱回填 → 已修**(R17 通讯块定界 + 邮箱按最近点名作者归属);现行评测作者类缺陷 0。
- **#116 td/th 多余 @style/@align → 已修**(表头发裸 th);**#186 表级 frame/rules 仍在**,见上文 B 类第一条。
- **#119 表脚注入 table-wrap-foot → 已修**(R17 补"缩写释义行"识别)。
- **#46 aff 放置位置 → 未改**,转入上文 B 类(现行评测不计,属与结构参考完全一致的可选项)。
- **#151/#153 back 命名小节 → 部分修**(前置区声明扫描、闭环 C 拆分);残余 5 条见上文 A 类。
- **#141/#143 刊名全称/DOI 补全 → B 档补全项**,现行评测单列覆盖率、不计结构分;`--crossref` 可选开启。
- **#69 段落归属、#70/#96 斜体语义与题注忠实、#79 Unicode 上下标等长尾** → 以现行 eval 逐条明细为准,
  未再作为跨样例系统性根因聚合出现(现行聚合见 `reports/eval/deterministic` 末尾"系统性根因"表)。

> 完整逐条缺陷清单(逐样例逐类:参考值 / 输出值 / 差异明细)与系统性根因聚合,由现行评测产出:
> `PYTHONPATH=scripts .venv/bin/python -m eval.run --llm off` → `reports/eval/<tag>/eval.{html,json,txt}`。
