# 14 例转换内容与缓存验收

当前状态（9 月 16 日 01:47）：代码 `c5f8a64` 与 28 组真实响应缓存已上线，14 篇稿件的 DeepSeek、Qwen 两种选择均已完成公网实际上传、预览、原稿对照和下载验收。全部最终首部截图及重点缺陷位置已实阅；剩余一项非阻断细节是 01/Qwen 的引文定位提示。完整结果与使用边界见末节“最终候选的公网验收”。中间各节保留修复依据和测试版本对应关系，不代表当前仍有历史失败。

## 目标和边界

目标是为评委测试与现场演示准备 14 篇已知 Word 稿件的稳定、高质量转换结果。网页提供 DeepSeek 和 Qwen 两种选择，因此验收对象为 28 组。原稿为内容依据，结构参考仅帮助定位差异，不是标准答案。

本轮不调整 Web 功能设计、不开展新一轮界面重构，不修改已提交材料，不制作 PPTX。采用真实模型响应的筛选、必要的重新生成与缓存预热；不改写模型原始回答，不伪造用量、检查状态或来源记录。已获授权修复有依据的转换内容与预览缺陷，不用缓存筛选代替程序修复。

本轮开始时的上线代码为 `45896ae`，工作树为项目 `tmp/web-release-r02`；开发起点为 `097f907`，工作树为 `tmp/web-next`。最终上线代码与回退位置见末节。用户对 AGENTS.md 的修改不覆盖、不提交。原有任务和旧缓存保持不变。

## 已确认的运行机制

依据 `webapp/app.py`、`webapp/workbench.py`、`src/word2jats/llm/cache.py` 和 `client.py`：缓存保存模型请求、回答及真实接口用量，普通上传仍执行 Word 解析、结构组装、校验和打包。旧任务 XML 的人工修改不会被下一次上传继承。重新转换使用独立缓存并重新调用模型，不能承诺与缓存复现相同。两种模型分别准备缓存；改变出版设置不应串用其他任务的信息。

## 固定顺序

1. 固定 Git 回退点，登记原稿摘要、候选任务、代码和缓存来源。审查历史记录，优先利用已有真实调用，受解析修改影响的 01 使用修复后的候选。
2. 在独立目录按当前代码复现 28 组，禁止新增网络调用，发现缺失就报告；保留完整回答和用量。检查格式、媒体、原稿覆盖、结构差异与实际预览。
3. 逐篇审查题名作者机构、摘要关键词、章节正文列表声明、图表表注、公式、引用文献和交付一致性。差异回到原稿判断，不按参考的相似度挑答案。
4. 对模型错误优先选择已有合格真实候选；必要时有界重新生成。不随意拼接互相依赖的多轮回答。程序缺陷与缓存缺失分别记录。
5. 合并候选后再次执行禁止模型网络请求的复现与重复运行校验，确认无缓存冲突、遗漏、用量缺失。公网验证逐一上传、完整预览、下载；额外检查改名和正常出版设置变化。
6. 仅在内容和运行检查通过后切换缓存；避免打断活动任务，保留旧配置和旧缓存。复核既有任务未变，停止临时服务，输出逐篇结论及剩余问题。

## 内容验收与收敛

必须排除题名截断、作者遗漏或关系错误、段落缺失或重复、章节明显错位、错图缺图、图题表注错配、表格数据遗漏、公式或特殊字符损坏、引用错连、文献串条、包内媒体不全等明显缺陷。合法的结构表达差异不强求与参考一致。

程序通过、DTD 合法、词量接近均不能代替内容审阅。机器检查用于定位疑点，逐篇核查结论与截图实阅分别登记。不得把未审内容、历史未审帧或跳过项算作通过。本轮不恢复历史百万帧审查，也不重跑无关的全部按钮排列。

证据统一保存在 `reports/sample-cache-20260915/`；脚本和维护文档纳入 Git，缓存及稿件不入 Git。现有网络设置保持不变；如外部环境影响测试，先检查，不自行更改网卡。

## 修复依据与版本验证记录

开始检查时已固定计划并保留回退标签 `web-cache-before-20260915`。以下按代码与候选版本记录当时的定位证据；最终结论在末节单独列出。

9 月 15 日新增授权：已确认必须修复导致内容遗漏的转换器代码。执行顺序为最小复现失败用例、内容组装修复、相同真实响应重放 28 组、逐篇内容与展示验收、正式发布与缓存切换。代码修复和候选缓存准备分别判定，不以筛选缓存代替修复程序缺陷。目标是在合规合理范围内尽量完善 14 篇展示；优先消除影响内容、观感和交付的明显问题，不为合理表达差异扩大重构。

### 已完成的隔离检查

`scripts/prepare_sample_cache.py` 从历史真实响应准备独立缓存 `cache-v1`，逐条校验请求摘要和用量是否存在，来源及同键替换登记在 `cache-v1-manifest.json`。初步缓存 1,721 条，当前发布后缓存优先于旧响应的替换为 133 条；这只是候选集合，不是通过验收的最终缓存。登记了 101 个已知稿件的历史完成任务。

28 组均以正式代码 `45896ae` 在禁止套接字对外连接的条件下完成重放，新增模型调用为零，用量记录完整。27 组 DTD 合法，X02/DeepSeek 的初始候选不合法。进一步对 11 组历史替代候选进行同样的离线重放，均得到可检查产物。未改写任何模型回答，未向模型发起新请求。

相关证据为 `replay-v1/*/result.json`、`alternatives/*/*/result.json`、每组 `source.txt`、`output.txt` 和 `content.json`。结构参考的数量差异仅用作定位，不直接判为缺陷；作者清单包含编辑贡献者，不能把总 contrib 数当作者人数。`source.txt` 从事实解析层导出，并非 Word 原生排版图像。

### 初始检查中阻止发布的问题

| 稿件与模型 | 已确认事实 | 当前处理结论 |
| --- | --- | --- |
| 05 / Qwen | 初始候选没有参考文献，原稿有 27 条 | 历史任务 `c01e0433464a4ed8` 及提交前 r4 响应可在当前代码复现 27 条；仍有扁平表格列结构警告，不能据文献恢复就判整篇通过 |
| X01 / Qwen | 公式存在，但所在原稿段落前后约 560 字符解释文字未进入输出 | 实际预览、原稿及 XML 对照已确认，见 `blockers/X01-dashscope.png` |
| X01 / DeepSeek | 同一段解释文字保留，但数学公式缺失 | 实际预览中 “following formula:” 后直接接文献引用，见 `blockers/X01-deepseek.png`；不能用它替代 Qwen 后声称问题已解决 |
| X02 / DeepSeek | 初始候选存在只有表头的表格；公式附近说明与部分图题未完整输出 | 可找到 DTD 合法替代候选，但内容遗漏仍存在，不能仅以格式合法选用 |
| X02 / Qwen | 初始及多个替代候选存在图题、图中文字、公式上下文遗漏等覆盖异常 | 已定位原稿节点；尚未逐一完成视觉审查，不判全部异常均为同一原因 |
| X04 / DeepSeek | 初始候选缺两处文字排版公式，以及文首位置的致谢和 AI 使用声明 | 两处公式遗漏已在实际预览核实；替代候选仍有未解决项 |

其中至少一项已证实为确定的组装代码问题：`understand/assemble.py::_display_formulas` 为一个公式将整个原稿节点加入 consumed，`_body` 随后跳过该节点，所以公式以外的文字不再输出。独立于模型的最小输入 `Before [formula] after.` 只得到 `Formula`，没有任何 `Paragraph`。复现命令为 `python scripts/reproduce_formula_context_loss.py`，当前退出码 1；这是一项已确认失败，不算测试通过。

`scripts/check_sample_content_blockers.py` 在本机只读展示服务中读取实际重放产物，4 组重点场景完成浏览器核查，4 张对照截图均已实际查看；首页截图也已查看。`blockers/evidence.json` 记录原稿存在而预览缺失的指定片段。X02 截图的左右滚动位置不同，文字是否缺失另由完整预览文本检查确认，不能只凭截图视野判断。此处没有执行 28 组逐篇完整视觉验收，也没有执行正式站点重新上传和缓存切换。

### 已批准的最小修复范围

已批准修复转换器的内容组装，不改变 Web 页面和交互：按源文字区间保留公式前后说明；补足文字排版公式的输出路径；图表容器只消费已实际输出的内容。每项修改必须有真实原稿复现和定向测试，再用已有真实响应重放全部 28 组。仅在证据表明属于代码缺陷时改动；模型错误仍走真实候选选择和必要的重新生成。现阶段不发布未达到内容验收条件的候选缓存。

公式修复已完成第一轮隔离验证：新增用例修复前 12 项失败、2 项通过；修复后连同嵌套公式、既有语义组装和渲染回归共 67 项通过，见 `formula-tests.xml`。`replay-v2` 使用开发代码和与初始重放相同的真实缓存，28 组均零新增调用完成。X01 / Qwen 的同段文字已恢复，X01 / DeepSeek 的公式已恢复，X04 / DeepSeek 的两处文字排版公式已保留。X02 的嵌套图和图题仍缺失，尚未发布；下一步修复其已确认的容器组装问题。

### 当前修复与候选收敛（9 月 15 日深夜）

公式修复已提交为 `4021979`。随后完成了以下有源稿与独立用例依据的修复，均尚未上线：

- Word 排版表中的子图按各自单元格保留图与说明，正文锚点使用可遍历的顶层节点；正文句子内的图片不搬到远处图组。
- 原生表格已被分类为表格时，不因缺少重复规格而整表遗漏；没有进入图表容器的题注文字保留在原位置。题注内公式只输出一次。整批重放曾发现 01 的重复公式 ID，已定位为残余题注重复输出并修复，`replay-v7-01` 两组重新通过 DTD。
- 公式编号、重叠图题按源地址避免重复，下划线按 Word 字符格式保留；只有表头的表使用合法直接行，不虚构表体。
- X01 的 30 条文献在 Word 中本来就是字面 JATS。新增完整片段解析，著录值仍保留原稿地址，实体解码单独核验，无法完整表达的片段不会静默删字。`replay-v5b-x01` 两组 30 条均结构化、源覆盖问题为零。
- 扁平表格显式空格与省略空格等价处理；行列总数与实际坐标不一致时，只有重新计算格网后仍通过全部源片段唯一归属、顺序、跨度、边界检查才采用。原始模型回答不改写。05 / Qwen 历史候选 `c01e0433464a4ed8` 在 `alternatives-v8` 恢复 14 张格网表、27 条文献，源覆盖问题为零、无扁平表退化提示。

已完成一次包含浏览器的全测试：`content-full-tests-browser.xml` 为 846 项通过、5 项已有条件跳过、1 项依赖弃用提示。这次结果早于字面 XML 文献和后续表格修复，不能替代最终版本回归。第一次未设置浏览器路径的全测有 5 项启动失败，记录保留于 `content-full-tests.xml`，不算通过。

为比较模型候选，对 05 和 X02 各新增一轮 Qwen 真实调用（分别 59、39 次请求），记录与准确用量见 `fresh-qwen-01/*/result.json`。新候选没有比已选历史候选更合适，因此没有因“新”而优先采用。其余重放全部禁止模型网络连接。

`scripts/select_sample_cache.py` 生成独立候选 `cache-v2`，含 1,752 条原始响应。以 `cache-v1` 为基础，仅按对应完整任务的实际使用键选择 05 / Qwen 的 `c01e0433464a4ed8`、X04 / DeepSeek 的 `cf6f25705c4e4787`；后者保留致谢、AI 使用声明和全部文献，仅剩原稿未填写的 Received 日期模板待判。来源与替换记录在 `cache-v2-manifest.json`。它还不是已发布或已完成逐篇视觉验收的缓存。

接续顺序固定为：当前代码 Git 提交 → 完整测试与 `cache-v2` 全 28 组禁止网络重放 → 每篇实际预览、图表公式文献和下载核查 → 必要的定向修复及复验 → 独立发布工作树及正式缓存切换 → 公网逐例上传下载核验 → 停止临时服务。不要重新开展 UI 重构，不做 PPT，不修改提交包，不因个别空白日期模板偏离展示和交付目标。

### 9 月 16 日凌晨：实际预览发现的追加问题

`e4bad7b` 的完整回归为 `final-code-tests-v1.xml`：873 项通过、5 项条件跳过。`selected-replay-v1` 全 28 组 DTD 合法、零新增模型请求。`scripts/review_selected_samples.py` 已逐组打开真实网页、确认全部浏览器图片加载、点击下载，并将包内 XML 与候选逐字节比对、将内层 `figures.zip` 的每个引用资源与原资源逐字节比对。28 组均通过这些功能检查；各目录包含首页及章节、图表、文献定位截图，**生成截图不等于逐图实阅**。

已实际查看的页面确认了两项追加问题：05 第三张生存曲线后的数值表在两种模型下均有列错位；X02 的 EMF 图只有原图下载入口而没有图像预览。当前开发树新增了以下修复，均未上线：

- 对单行表头、明确空白行标标题、各行相同数量独立数值的完整矩阵，按原始片段恢复列对应；不推断稀疏表、多层表头或自由文字。原回答和所有数值不变，原归属与新格网都须通过完整性校验。`replay-v9a-05` 两组相关表均为 5 列，值逐行一致；`selected-preview-v2/05-dashscope/content-017.png` 已实阅。最初定向测试暴露缺少 `re` 导入，`replay-v9-05` 的失败记录保留，不算通过。
- 用本机已有 LibreOffice 在独立用户目录中解码 EMF，生成按内容摘要缓存的 PNG 预览。限定输入体积、输出像素及进程时长，超时停止该独立进程组，完成后清理临时目录；失败保留原图入口。只裁掉完全空白的画布，保留可见像素，下载包中的原始 EMF 不变。原稿对照与转换预览共用解码结果，不新增按钮、不改变界面设计。X02 两组的可显示图片从 26 个增至 41 个，下载字节比对通过；`selected-preview-v2/X02-dashscope/content-010.png` 已实阅。
- 已归并为图文摘要的对象不再留下空正文图规格；仅为已确认图文摘要图保留紧前的独立原稿标题，不猜其他正文归属。

上述合并代码完整回归为 `final-code-tests-v2.xml`：**892 项通过、5 项跳过、1 项依赖弃用提示**。跳过原因分别为一个不适用的等价变换、一个没有默认输出的评估用例、3 个旧评测目录未预热的样例；后者不能当作新缓存测试，当前样例已由独立 28 组真实重放覆盖。

`selected-replay-v2` 全 28 组再次 DTD 合法、零新增模型请求。`selected-preview-v2` 的 05、X02 共 4 组重新完成网页与下载检查。其后增加了“未填写出版日期模板”的独立识别：只处理正文开始前、没有其他语义归属、完整匹配空白日期形式的独立段落；不补日期，不处理实际日期或正文，独立源覆盖检查再次核验完整文字。对应 55 项定向回归通过，`selected-replay-v3` 正在执行；该增量尚未计入 892 项结果。

接续时优先核查：X02 第一个反应式是一个悬浮箭头叠放在 MathType 公式上，原稿 `doc/p42` 两个对象，现有预览将它们拆开，尚未解决；X04 `doc/p62–63`、`p68–69` 是下划线与空格排版的分式，下划线已保留，但必须在对应位置实阅。不要把这些问题与普通公式缺正文的已完成修复混为一谈。其余样例按首部、图表、公式、文献与源稿对照收敛，避免扩展到 UI 重构。

当前临时服务为 `selected-preview-v2`，进程 2096015，仅监听 127.0.0.1:18643；正式服务仍为 `45896ae`，没有重启或切换缓存。候选仍是 `cache-v2`，原始模型回答未修改。用户对 AGENTS.md 的修改仍单独保留。

### 当前接续状态：复杂公式已恢复原稿排版

`be98e5e` 包含数值矩阵、EMF 预览与空日期模板处理，对应 `final-code-tests-v3.xml` 为 909 项通过、5 项条件跳过。其后新增了严格限于源版式明确且正文输出边界闭合的原生公式排版：独立复合公式中的悬浮箭头与 OLE 主体保留为整体，两行下划线分式保留分子分母相对位置。对全部 14 篇的识别范围仅为 X02 `doc/p42`、X04 `doc/p62–63` 和 `doc/p68–69`，没有按样例编号写判断。

该处理从原始 Word 段落复制排版，不识别或改写数学内容；去除片段副本的页眉页脚与行号，源媒体和公式对象字节不变。隔离 LibreOffice 用户目录，有时长、文件和像素上限，失败保留原有输出。PNG 作为派生资源写入实际交付包，源字符仍逐字写入 `alt-text`，原稿摘要、段落、原对象和 PNG 摘要单独登记并核查，不冒充原始图片。

`native-replay-v1` 的 X02、X04 共 4 组均 DTD、源覆盖与来源检查通过、零新增模型请求；`native-preview-v1` 的 4 组网页图片与下载字节比对通过。三张原生排版图和 X02/DeepSeek `content-003.png`、X04/DeepSeek `content-002.png` 已实际查看，箭头、分式和编号位置正确。定向 `source-layout-tests-v2.xml` 27 项通过，包括拒绝普通正文、排版失败回退、派生证据篡改拒绝、跨引用实体保留、临时目录清理和自身进程超时终止。

含原生公式处理的完整回归 `final-code-tests-v4.xml`：**929 项通过、5 项既有条件跳过、1 项依赖弃用提示**。`selected-replay-v4` 全 28 组完成，均 DTD 合法、零新增模型请求。尚未切换正式服务和缓存。

内容复核继续检查各篇首部、图表、文献。S01/Qwen、S02/Qwen 等出现 `BIBR_XREF_AMBIGUOUS`，已核实为模型引用上下文与原稿不一致造成的定位失败，引用文字仍在；下一步仅研究能否按原稿明确的数字引文与实际文献编号无歧义定位，不猜歧义目标、不直接删提示。S04 作者后的 `#` 本来在原稿中但未附解释，不补写其含义。摘要前短虚线是现有通用预览样式，不属于内容遗漏，本轮不为此重构界面。

现有临时展示服务：127.0.0.1:18643（`selected-preview-v2`）、127.0.0.1:18644（`native-preview-v1`）；正式服务 PID 1878839、代码 `45896ae`、重启次数 0。浏览器路径是开发树 `reports/web-next/browsers`，旧 home 路径已失效。脚本重放必须设置 `W2J_REVIEW_CODE_ROOT` 为开发树并使用新输出目录。候选仍为 `cache-v2`，不再使用已知有缺陷的 `cache-v1` 上线。

### 发布前收敛状态（9 月 16 日 01:20）

当前以决赛演示和实际交付为收敛标准，不追求参考 XML 的逐标签相同，不扩展前端重构。`4ffeb74` 后追加的修改均有原稿、实际页面或最小测试依据：

- 数字引文只在原稿同一记录的方括号位置、文献实际编号和上下文能够唯一确定时补链接；其他同目标引用已独立定位的区间可用于排除。无依据的定位不猜。`citation-replay-v2` 中 01/DeepSeek、04 双模型、S02 双模型的漏定位提示已消除；01/Qwen 仍有一处重复的 `[39]` 无法唯一定位，文字完整，不影响转换下载。
- 预览样式表原来把定位锚点插入 `colgroup`、`col` 等不允许含链接的表格容器，浏览器修复 HTML 时多算一列，导致列宽错位。现在仅在单元格中保留附加锚点。S01、S03 的双模型真实页面及下载通过，`selected-preview-v5` 的两个首表截图已实阅，S03 问题列恢复宽列。
- Word 超链接按字符格式拆成“空格”和“网址”两段时，预览原来把空格也展开为目标网址。现在空白仍显示空白，真正没有子内容的链接仍可显示目标网址。04 的实际图注已复验，网址不再连续重复，交付 XML 没有改写。
- 默认不填期刊时，自动结果缺少期刊标识和 ISSN，界面提示“补充出版信息”正确；填写保存通过格式检查后，却仍继承原始 `delivered=False`，产生没有内容依据的泛化警告。修复只在完整原始检查记录证明“唯一失败是 DTD”、且当前保存版本校验成功时解除旧提示，原始自动转换结论及其他未解决问题保持不变。

`selected-replay-v5` 全 28 组已完成：均零新增模型调用、用量完整、DTD 与全部 11 项检查通过、源覆盖问题为零。实际上传文件名为服务器的 `input.docx`、出版信息为空的独立重放 `web-default-replay-v1` 同样全部缓存命中；该情形不声称 DTD 已通过，缺失期刊需由用户补充。出版设置不会作为缓存选择的暗中前提。

`final-code-tests-v6.xml` 为 951 项通过、5 项既有跳过；加入空白链接修复的 `final-code-tests-v7.xml` 为 955 项通过、5 项跳过，均有一个依赖弃用提示。补齐期刊后旧提示的修复另有定向测试，仍需计入最终完整回归，不混算已有数字。

真实隔离上传脚本为 `scripts/verify_sample_cache_public.py`：通过网页上传不变的源文件字节（改名以验证不依赖文件名）、选择模型和出版设置、校验真实缓存与用量、逐字节对照已验收 XML 和媒体、检查全部预览图像及内部链接、打开原稿对照、下载成果和原稿、刷新核对保存版本。额外的 `--default-publication` 路径从空出版设置开始，再在网页补充保存。它不点击“重新转换”，不发起预期外付费调用，截图生成与实阅结论分开登记。

`local-upload-v1` 的 04、X02 双模型已从网页完成上述全流程；04 图注截图已实阅。`local-default-v2` 的 S03/DeepSeek 已完成补充出版信息和下载，但截图暴露旧失败提示，因此不是最终通过依据；修复后复验存放 `local-default-v3`。第一次脚本把“未作修改”误期待成“已保存”，只修正脚本，失败记录留存，不算产品失败或测试通过。

内容实阅覆盖了全部 28 组首部（分布在 `selected-preview-v1/v2`、`native-preview-v1`），并补看了各篇首表、首图和文献开头，复杂公式、嵌套图及数值矩阵按已确认缺陷位置复验。没有声称目录中每一张生成截图均已实阅。模型间参考文献部分采用混合著录、X02 公式分组数不同等，不等于内容缺失；所有原稿覆盖和媒体核验仍须通过。

正式服务仍是 `45896ae`，生产缓存没有切换。接续顺序：结束当前定向浏览器检查 → 最终完整回归并固定 Git 提交 → 独立 `web-release-r03` 工作树与新生产缓存 → 核对没有活动任务、保存既有任务与配置摘要 → 切换并立即验证公网首页及 28 组真实上传下载 → 补充出版信息与复用复验 → 确认旧任务、提交包不变并停止临时服务。生产目录、网络和已有稿件不做无关改动。

### 第一次发布与公网补充检查（01:22 起）

`5482672` 完整测试 `final-code-tests-v8.xml` 为 962 项通过、5 项既有跳过、1 项依赖提示。已发布到 `tmp/web-release-r03`，生产缓存为 `webapp/_cache_samples_20260916`（1,752 条原始回答和 3 个原生公式排版 PNG）。首次切换前 132 个已有任务没有活动转换，任务快照、源稿、当前 XML、旧缓存和已提交 ZIP 均已记录摘要；切换后的 `release-safety-after.json` 核验未变。此后不再声称生产仍是旧版本。

`public-upload-v1` 的 01 双模型通过真实公网上传、用量、原稿、预览、下载及 XML/媒体一致性检查。02/DeepSeek 暴露一处原有预览缺陷：多目标文献引文中的某条是混合著录，编号保留在条目正文而没有独立 `label`，预览未展开多个目标，生成了带空格的无效链接。横向扫描全部 28 组，只有 02/DeepSeek 和 S05/DeepSeek 出现同类情况。交付 XML 的引用关系正确，修复仅在预览中读取条目开头明确的 `[19]`、`19.` 或 `19)` 编号，不从实体 ID 或次序猜编号；不修改条目本身。修复后 28 组生成的预览内部链接全部有实际目标，另有 65 项定向回归通过。

`public-default-v1` 的 S03、X04 双模型已从空出版信息开始，经网页补充、保存、预览和下载通过，原始用量不变，旧泛化警告已消除。公网补充检查的新轮次会包含实际点击引用与等待滚动定位，不以链接存在代替点击验证。`local-links-v1` 初次等待固定 250 ms，在长文平滑滚动结束前判断失败；脚本改为等待目标真正进入视口，复验存 `local-links-v2`，不据此修改页面滚动设计。

下一次发布使用独立 `web-release-r04`，仍使用同一已验收缓存，XML 内容不变。先结束链接定向验收和最终整套回归，再切换，随后重新完成公网 28 组。正式服务在准备期间保持 r03，旧任务和已提交材料继续保持不变。

### 最终候选的公网验收

9 月 16 日 01:33 已切换到 `c5f8a646d762573d0a31733705f5eec777ecd0e8`，正式运行工作树 `tmp/web-release-r04`；服务 PID 2172068，生产缓存仍为 `_cache_samples_20260916`。代码完整回归 `final-code-tests-v9.xml`：**969 项通过、5 项既有条件跳过、1 项依赖弃用提示**。`release-safety-after.json` 已再次确认 132 个原有任务、旧缓存、原始模型响应和提交 ZIP 未变，发布工作树干净。

完整公网验收 `public-upload-v2` 已于 01:46 前完成，`summary.json` 为 **28／28 组通过**。每组都通过 `https://word2jats.jianglab.work` 上传不变的原稿字节，文件统一改名以排除对原文件名的依赖。新增模型请求、缓存缺失与浏览器脚本错误均为零；每组原始输入、输出和缓存用量完整显示。已逐字节核对下载 XML、228 处媒体引用对应的资源以及下载的原稿；预览中的全部图片成功加载，没有图片降级占位，内部链接均有目标。另实际点击文献链接并等待目标进入视口，02/DeepSeek 专门点击已修复的第 19 条文献。01 双模型的重复上传 XML 和原始用量与上一轮逐项相同。

`public-default-v2` 的 S03、X04 双模型共 4 组已全部通过从空出版设置到补充保存、预览、引用跳转、原稿对照和下载的真实公网流程。保存后 DTD 通过，原始用量不变，旧泛化提示消除。

本次公网复用的服务端转换用时如下，单位为秒；不含浏览器上传、预览加载及下载时间，不是首次调用模型的耗时。两种模型均逐篇完成了上述流程，表中的用时不是质量评分。

| 稿件 | DeepSeek | Qwen |
| --- | ---: | ---: |
| 01 | 2.6 | 2.0 |
| 02 | 1.4 | 1.3 |
| 03 | 1.9 | 1.5 |
| 04 | 1.9 | 2.4 |
| 05 | 1.6 | 1.5 |
| S01 | 0.8 | 0.8 |
| S02 | 1.4 | 1.5 |
| S03 | 1.0 | 1.0 |
| S04 | 1.3 | 1.2 |
| S05 | 1.5 | 1.5 |
| X01 | 4.5 | 4.4 |
| X02 | 1.2 | 1.1 |
| X03 | 1.0 | 1.0 |
| X04 | 1.6 | 1.4 |

视觉检查与自动化断言分别记录：`public-upload-v2/*/03-result.png` 的全部 28 组首部均已实阅；另实阅 X01/Qwen 的公式及后续解释、X01/DeepSeek 文献、X02/Qwen 原生反应式、X02/DeepSeek 子图、X04 双模型两处分式、S03/DeepSeek 首表、02 与 S05/DeepSeek 引用跳转、01/DeepSeek 用量页面。补充出版信息流程中，S03/Qwen 保存后页面和 X04/DeepSeek 分式也已实阅。此前 05 数值矩阵、04 图注网址等缺陷位置的实阅依据保留在前文；其最终交付内容已逐字节复核一致。没有将目录中未逐一查看的其他截图标为已审，JSON 中的 `visual_reviewed` 不用于自动宣告全帧通过。

使用时需区分三件事：

- 正常上传这 14 份原稿并选择对应模型，可复用本次已验收的真实响应；Word 解析、XML 组装、校验与打包仍实际执行。仅改文件名不影响复用。
- 不填出版信息也能转换和下载，但缺失期刊信息时仍会提示补充；应按实际期刊填写，不凭空补造。额外 4 组公网流程已验证补充保存后的结果。
- “更多”中的“重新转换”会重新调用模型，不属于缓存复现。01/Qwen 尚有一处 `[39]` 的模型上下文不能在同段两个相同位置间唯一确定，因此保留“文献引用需要核对”提示；文字与第 39 条文献完整，DTD、预览与下载通过，未把未定位引用伪装为已解决。

最终安全核验 `release-safety-after.json` 确认 132 个本轮开始前的任务、旧缓存、原始响应和提交 ZIP 摘要均未变，正式工作树干净。服务为 active/running、自动重启次数 0；公网首页返回 200，最终版本运行日志没有 Python 回溯或 ASGI 异常，检查时无活动转换。测试浏览器已随脚本正常退出；本轮临时服务 18643、18644、18645 均无监听。不修改网络或其他服务，不在 home 顶层新建临时文档。

正式配置为 `scripts/deployment/web-samples-20260916.conf`，代码工作树 `tmp/web-release-r04`，生产缓存 `webapp/_cache_samples_20260916`。原有 r02 工作树、缓存 `_cache_targeted_20260915` 和配置 `scripts/deployment/web-public-20260915.conf` 保留用于回退。安全复核命令为：

```bash
.venv/bin/python scripts/verify_sample_release_safety.py after --code c5f8a646d762573d0a31733705f5eec777ecd0e8
```

结论：已确认影响本轮展示与交付的内容遗漏、公式排版、图像预览、表格列错位和引用跳转缺陷均已修复并上线；28 组正常上传路径及额外 4 组补充出版信息路径验收完成。01/Qwen 的单处引用定位提示作为非阻断边界明确保留。本轮停止扩展修改，当前代码和缓存作为决赛使用版本。
