# Web 产品收尾：范围、实施与验收

## 当前任务与恢复入口

用户已经提交决赛材料。本轮获批的是产品审视后的有限收尾，不是再次重构整个前端。每次上下文压缩后，先读本文件、AGENTS.md、用户最新指令，再接续当前状态。

Web 服务需要转换 Word、但不愿接触代码和部署的用户。主线为上传、自动结构化、查看原稿与结果、必要的关键字段修改、下载当前成果。人工操作必须服务于结果；不增加逐项打勾、签收、审核流或质量评分。统一使用 Word2JATS、Word、JATS XML、DeepSeek、Qwen。

业务依据是比赛选题一（章节、作者、摘要关键词、图表、公式、文献的结构化识别），评分以最新决赛通知的 25/30/25/20 为准。原始会话首条长指令强调完赛与竞争力，避免无限完美主义。已提交说明书明确限定关键字段修订，不承诺全文在线编辑。

## 本轮固定范围

1. **必须完成：编辑与原稿对照连贯。** 同篇稿件内切换原稿、文章信息、出版信息、检查或用量，不丢草稿、不迫使提前保存。保留编辑位置和原稿阅读位置，提供明确的继续编辑入口。离开稿件、刷新、主动取消编辑、恢复结果等真正会丢弃输入的动作仍受保护。保存失败、其他页面更新、窄屏和键盘使用必须覆盖。
2. **次优先：有限的结构查看。** 沿用目录与预览，增加按内容类型查找的入口和必要的结构说明；只反映当前保存 XML 中的事实，不当作正确率、完整率，不引入新的人工待办。覆盖文章信息、摘要关键词、章节、图表、公式和文献，不只强调参考文献。
3. **次优先：重新转换的出版设置。** 区分已经确认的出版设置和待重新识别的内容，优先沿用当前保存的出版信息。仍保留首次设置的明确选择和原结果，不带入题名、作者等内容修订。若实现需要改变转换核心或扩大出版配置体系，则记录依据并延后，不阻断主项收敛。

不做全文编辑、作者/单位增删、正文图表公式编辑、批量管理、账号权限、审核、PPT、说明书或提交包更新。不得因为验收发现无关问题扩大任务。

产品审视依据包括 18640 实际操作：编辑中切换原稿会弹出放弃输入；同篇真实结果有相同文献条数而承载形式不同；重新转换明确使用首次出版设置。行业资料用于界定转换与后续出版加工的分工，不把 PMC 入库要求强加给比赛原型：

- [JATS Publishing 定位](https://jats.nlm.nih.gov/publishing/index.html)
- [PMC 数据要求](https://pmc.ncbi.nlm.nih.gov/pub/min_requirements/)
- [eLife 的转换后编辑工具设计](https://elifesciences.org/labs/8de87c33/texture-an-open-science-manuscript-editor)

## 隔离与 Git

- 工作区 `/home/denggf/word2jats-web-next`，分支 `web-next-20260915`，本轮基线 `4d595c2`（标签 `web-next-preview-20260915`）。新增收尾前恢复标签，不移动原标签。
- 原工作区 `/home/denggf/学术期刊结构化技术创新大赛` 只读；正式服务 8504、PID 773812、启动时间 2026-09-14 20:50:09 不变。原提交 ZIP 的 SHA256 为 `ef4fc872c1f48295961fe264b498b0581b8808d58ee9839bfde732eafd2f3a41`。
- 用户已有 AGENTS.md 修改保留、不提交。本轮只提交自己的文件。
- 独立树临时服务 18640 可以更新，最后保持运行；测试使用独立夹具 18641 和 `reports/web-closeout/`，不修改本轮开始前已有的真实任务（最终执行前共五份）。
- 不调用 A800，不部署到正式域名，不使用子代理。密钥不输出。

## 执行顺序与验收

先提交计划与恢复标签，再实现主项及有界次优先项；补测试后启动独立夹具，浏览器走查，修复首个失败边界再继续。完成全量回归、独立截图视觉检查及最终临时服务验收，最后提交代码和更新本记录。

本轮新增场景：

- C01 作者、题名、单位关联、通讯字段在对照往返中保留；原稿和编辑阅读位置可继续。
- C02 文章信息与出版信息切换共同保存；预览和下载不展示未保存输入。
- C03 检查、用量、下载说明及关闭原稿后仍可继续编辑；主动取消与离开稿件仍确认。
- C04 保存失败、版本冲突、异步保存期间切换、刷新与任务切换保护。
- C05 桌面、笔记本、平板、手机的继续编辑入口、焦点、滚动、布局与提示。
- C06 各内容类型定位、结构说明、空结果和长列表；与实际 XML 对照，不修改 XML。
- C07 当前出版设置/首次设置的重新转换、未保存保护、旧任务保留及失败重试。

既有 76 项功能矩阵继续回归；测试数量、场景覆盖和代码覆盖分别报告。新旧相关视觉状态重新截图并实际查看，不能以生成截图冒充验收。沿用现有脚本时只调整已批准改变的行为，不删场景降低要求。核心转换未变，不为回归重复消耗所有样例的模型额度；真实新任务只在必要的端到端边界验证时运行。

## 当前进度

2026-09-15：三个范围项均已完成并通过验收，可以收敛。计划提交为 `b726e5f`，恢复标签为 `web-product-closeout-baseline-20260915`；完成版本用标签 `web-product-closeout-20260915` 定位。上一轮记录在 [Web 后续改进](Web后续改进-20260915.md)，本轮证据单独保存在 `reports/web-closeout/`。

### 已完成的产品行为

- 编辑与原稿对照：同篇稿件共用草稿，文章信息、出版信息和只读面板之间切换保留输入及阅读位置。编辑表单提供原稿入口，只读面板和结果顶部提供继续编辑。离开、取消、恢复和重新转换仍保留必要确认；保存失败、版本冲突及异步保存保护不变。
- 结构查看：沿用目录和预览，增加内容类型筛选。数量、关键词、公式表示形式、表格结构和文献著录形式均取自当前保存的 XML；不增加质量评分或人为待办。28 份冻结真实结果逐一检查新定位目标存在、结构清单与 XML 相符、原内容不变。
- 重新转换：默认沿用当前已保存的出版信息，可选择首次上传设置；未补齐的期刊信息不以空白覆盖新识别内容。新任务单独应用出版设置，不覆盖转换器原文件；题名、作者等文章内容仍重新识别。连续重新转换仍能回到最初上传设置。

没有扩大编辑范围，也没有修改核心 `src/`、说明书、提交包或正式服务。用户 AGENTS.md 的既有改动保留、不纳入提交。

### 本轮验收

| 验收范围 | 结果与依据 |
|---|---|
| 全量 Python 测试 | 718 通过、5 条件跳过、0 失败；`full-regression.txt`。跳过项与基线相同，不算作已执行的集成验证 |
| 新增后端测试 | 33 通过，已包含在全量结果中；`tests/test_web_closeout.py` |
| 功能场景 | 83/83：原 64 项、上一轮新增 12 项、本轮 C01–C07 全部覆盖 |
| 浏览器场景 | 原 61 项、新流程 N01–N11、本轮 C01–C07；分别见 `regression/验收清单.json`、`expanded/验收清单.json`、`product/验收.json` |
| 其余四项功能 | P11 重启恢复、N12 服务隔离见真实验收；E11 未改内容保留与 E18 非法输入拒绝见全量后端测试 |
| 独立视觉验收 | 181/181 张截图，180 张不同图，45 张图版均实际查看；`visual-review/manifest.json` 保存原图 SHA256、尺寸及人工查看记录 |
| 无障碍与键盘 | 18 个宽窄屏状态未发现 axe 自动规则违规；另检查窄屏焦点约束和返回编辑，不等同于完整无障碍认证 |
| JavaScript 单元测试 | `node --test --experimental-test-coverage tests/web_session.test.cjs`：4 通过；近期记录模块行覆盖 97.62%、分支 97.78%、函数 100% |
| 真实模型与重启 | DeepSeek、Qwen 均执行真实新调用，确认出版设置继承、文章内容重新识别、XML 与成果包一致，重启后结果和用量保持；`real/验收.json` |

Web Python 语句覆盖为 1323/1428（92.65%），分支覆盖为 463/532（87.03%）；下载模块语句与分支均为 100%。功能场景全覆盖和视觉状态全覆盖不等于全部代码覆盖 100%。覆盖数据见 `coverage.json`。

视觉检查覆盖首页、上传及失败、处理进度、结果、目录、原稿、文章与出版编辑、保存及版本冲突、下载及失败、真实结果与计量。新增 34 张截图包含六种屏幕尺寸下的草稿往返，以及各结构类型和重新转换选项。原尺寸另复查手机编辑、草稿保留时的原稿、公式及文献定位。定位截图在跨框架消息处理完成、目标选中并进入可视区域后生成，不能只等待旧的选中元素存在。

真实验收只修改本轮脚本创建的三份任务：`5ec4d889283042da`、`bb55e287a57d443a`、`9acffcf534854e50`。服务重启后逐份核验结果版本、出版信息、用量及成果包；执行前已有的五份任务版本均未改变。测试副本中加入的题名或 DOI 仅用于断言，不作为演示稿。

### 当前服务与后续边界

真实预览继续运行在 [局域网 18640](http://10.39.12.242:18640) / [Tailscale 18640](http://100.104.251.41:18640)，用户级服务为 `word2jats-web-next-20260915.service`，本轮最终重启后 PID 为 1271374。测试夹具 18641 与本轮专用浏览器在验收后关闭，保留证据文件；不影响预览服务。

正式服务仍为 PID 773812，启动时间与本轮开始时一致；原工作区仍停留在 `bbf05b1`，仅有用户原来的 AGENTS.md 修改。已提交 ZIP 的 SHA256 与上述基线相同。后续是否替换正式站点须另行确认，不自动部署，也不再次重做整个前端。

本轮使用说明已同步至 [Web 应用](../08-Web应用.md)。重跑新增浏览器验证时，使用独立夹具和 `scripts.verify_web_closeout`；真实测试脚本 `scripts.verify_web_closeout_real run` 会创建任务并调用模型，`after` 用于服务重启后的核验，不能当作无副作用的健康检查。
