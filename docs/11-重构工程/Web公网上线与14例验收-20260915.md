# Web 公网上线与 14 例逐项验收

## 当前授权与恢复入口

2026-09-15 用户批准替换正式网站，并要求上线后从公网前端对全部 14 例执行全新调用、逐例全功能操作及密集连续截图检查。每轮先完成全部测试，再汇总分析、统一修复；修复后重新上线并重跑全轮，直至没有新问题。此指令替代上一轮“不部署正式站点”的限制。每次压缩上下文后先读用户最新指令、AGENTS.md、本文件和本轮进行中记录。

目标仍是可靠地完成 Word → JATS XML、查看对照、有效的关键字段修改和成果下载，不扩展为出版管理系统，不重做已提交材料，不增加形式化核对。用户已有 AGENTS.md 不提交、不覆盖。不启用子代理。

## 上线与回滚

- 待上线应用基线：`87d075b`，原开发树 `/home/denggf/word2jats-web-next`。
- 原正式源树：`/home/denggf/学术期刊结构化技术创新大赛`，`bbf05b1`；保持其代码和已提交材料不变。
- 正式入口：`https://word2jats.jianglab.work`；systemd `word2jats-web.service`、端口 8504。保留现有隧道与域名。
- 使用单独的只含本轮固定版本的发布工作树，避免开发中修改静态文件直接影响正式用户。服务覆盖配置指定发布代码和原正式运行数据目录，原任务链接继续可用。
- 切换前记录既有任务状态、当前 XML 哈希、计量和服务配置；有转换中任务则待其完成。建立 Git 恢复标签；服务覆盖配置只新增本轮文件。删除本轮覆盖配置、daemon-reload、重启原服务即可回滚。不要删除其他配置。
- 每轮上传使用新的服务端缓存目录，既有缓存不清空；首次转换必须核对真实调用与新增用量。重新转换使用应用本身的独立任务缓存。不把模型供应商的输入缓存命中误当成本地结果复用。

## 本轮验收范围

唯一样例名单取 `样例数据/样例登记.json`：01–05、S01–S05、X01–X04。每例覆盖 DeepSeek 和 Qwen 的新转换结果，所有操作通过公网浏览器入口完成，额外请求只用于核对浏览器所产生的任务和文件。故障模拟仅作用于测试浏览器，不破坏全站或真实用户任务。

逐例检查：

1. 首页、键盘入口、文件选择/拖入/替换/移除、多文件/格式/空文件/超限、两种模型、出版信息展开/收起及填写、开始转换。
2. 分片上传、失败重传、上传中导航保护、处理进度、复制链接、返回首页、最近记录再进入、刷新及断线恢复；转换失败和失效链接入口。
3. 结果页、全部顶栏及更多菜单入口、预览/XML 切换、目录收放、六种结构类型及搜索空结果；逐条点击该稿件实际目录，检查定位与内容显示。预览与原稿分段连续滚动，覆盖全文、图片、表格、公式、文献。
4. 原稿对照与下载、所有文章/出版字段和可用选项、往返保留草稿/滚动位置、保存/取消、各离开入口的确认与取消；只读面板的继续编辑；保存失败重试、版本冲突和异步保护。
5. 检查详情及实际提供的每个定位/修改/技术展开入口；用量的实际接口统计与导出一致。
6. 成果包、单独 XML、Word 原稿、文件说明与链接复制；文件名、ZIP 内容、XML 字节/版本、配套媒体一致；未保存提示、下载失败、旧版本提示。
7. 恢复自动结果（取消/确认），重新转换（模型切换、当前/首次出版设置、取消/确认、旧结果保留）；近期记录打开/移除/跨标签同步。
8. 桌面、笔记本、平板横竖屏、手机横竖屏中的实际结果和各面板；键盘焦点、Escape、Tab 往返、横向溢出、遮挡、阅读和按钮可用性。

无此内容的稿件检查对应空态并记为不适用，不假造图表或作者。可无限重复的操作不可能穷尽所有排列；通过逐例功能、状态分支和往返组合的明细矩阵验收，不把有限矩阵宣称为数学意义的全部路径。新增发现的路径补入矩阵，下轮全量重跑。

## 截图与收敛纪律

操作前后截图，操作过程中记录浏览器提供的连续渲染帧，保留时间戳、动作、任务和失败证据；长内容逐屏滚动覆盖，不只截首屏。浏览器连续帧接口不保证每个硬件刷新帧，须如实记录实际帧数和频率。所有采集的视觉状态实际打开审查；完全相同的图像可用哈希对应同一图，不能自动标记未看的图为通过。

每轮证据独立存 `reports/web-public-20260915/round-XX/`，包括操作日志、截图/连续帧、下载、任务/用量、矩阵及问题表。发现问题只记录并安全恢复测试现场，完成该轮后统一分析修复；阻断路径标明，不假报通过。验收脚本自身问题与产品问题分开登记，修复脚本后补跑被阻断的场景，不丢弃已有证据。

全量 Python/JavaScript 回归和覆盖率另行报告，不能将业务矩阵 100% 偷换为代码 100%。每轮须同时满足 14 例双模型新调用、逐例矩阵完成、所有截图审查完成、下载一致性及回归通过，且无未解决产品问题，才可报告收敛。API 额度或网络等外部阻碍如实说明，不复用旧结果替代全新转换。

## 当前进度

- 已重新核对上一轮完成记录、实际源代码、正式服务、14 例登记及原始需求。
- 原正式服务 PID 773812；14 个已有任务快照待逐份切换前核验。
- 原提交 ZIP SHA256：`ef4fc872c1f48295961fe264b498b0581b8808d58ee9839bfde732eafd2f3a41`。
- 已上线：固定发布树 `/home/denggf/word2jats-web-release-r01`，应用 `87d075b`，正式服务 PID 1304807；新增覆盖配置 `/etc/systemd/system/word2jats-web.service.d/zzzz-public-20260915.conf`。公网首页已实际截图检查；14 个原任务状态、XML 哈希、完整 llm 统计逐份核对一致。
- Git 计划提交 `175e92d`；标签 `web-public-before-20260915` 指向原正式版本，`web-public-candidate-20260915` 指向待验收应用。覆盖配置源文件为 `scripts/deployment/web-public-20260915.conf`。
- 第一轮进行中。`scripts.verify_web_public_convert` 从公网界面逐例双模型上传，`scripts.verify_web_public_actions` 对本轮新任务执行结果页操作。证据在 `reports/web-public-20260915/round-01`；不能把已有 `complete.json` 当成视觉验收完成，所有视觉审查尚待完成。
- 连续帧采用浏览器 screencast `everyNthFrame=1`，保留实际提供的每帧及时间戳；完全相同 JPEG 只保存一份但日志保留每帧关联。操作前后另有 PNG，正文逐屏滚动。采集期间证据会较大，应监测空间，不擅自删帧或抽样冒充全量。
- 首批 01 双模型已成功真实调用：DeepSeek 50 次/544092 Token，Qwen 51 次/578622 Token。02 已提交新调用。发生一次原稿核验请求超时，随后暴露测试记录器同名参数异常，导致测试脚本中断；只修复了测试记录器并提高文件下载核验超时，没有修改线上应用。02 接续原已发起的新任务，不再次上传复用缓存。中断前证据保留，续跑写入独立 continuation 目录。

### 12:09 接续记录（仍为第一轮，尚未统一修复）

- 原应用发布仍为 `87d075b`、PID 1304807。开发树只有验收脚本/记录更新，最新提交 `d1c2a14`；之后有少量测试脚本修改待提交，用户 AGENTS.md 仍不纳入。
- 正在运行的转换进程来自会话 90339（旧版 `verify_web_public_convert`，并发 2）；已推进至 S01，03 双模型实际转换完成，但 37 MB 原稿的辅助 API 下载超时，`convert/result.json` 尚未建立。重跑该脚本会通过现有 `task.json` 接续同一份新调用，禁止重传缓存代替。最新脚本已改为点击公网前端“下载 Word 原稿”后核对字节，与真实用户同一路径；不修改正式应用。
- 正在运行的结果页进程来自会话 98285（启动时只发现 01/02 四份成功记录）；01 已执行至响应式/恢复/重转。该进程加载的是补充前旧脚本，后续必须补跑 `remaining_entries`，并重新覆盖脚本时序问题阻断的路径。不能因 `complete.json` 存在就宣称该例全部通过；文件中的 issues、各步骤日志、视觉审查都要汇总。
- 基础回归：718 passed、5 skipped、1 个既有弃用警告，158.58 秒；本轮 `pytest.xml`、`coverage.json`。JavaScript 4 passed，session.js 行覆盖 97.62%、分支 97.78%。代码未变，线上后续功能问题修复后仍须全量重跑。
- 实际已查看 `visual-review/01-convert-screenshots/sheet-00001.jpg` 至 `00015.jpg`，98 张操作截图、59 个精确不同图，已登记。只有这批登记为已看；其他截图和所有连续帧仍未完成审查。初始 about:blank 是导航前证据，不算应用白屏；Qwen 首次结果出现编辑/目录暂未载入的退化状态，需结合请求时序继续核实，不能直接当作正常完整结果。

暂记问题（完成全轮后统一归因与修复）：

1. **产品恢复问题**：模拟原稿 503 后，关闭/重开对照继续复用同一个失败 iframe，不触发重新请求；下载 Word 可用。代码 `drawSource` 只在 frame 不存在或任务改变时设置 src，实际双模型重现。不得在本轮中途修复。
2. **测试时序**：原稿自动定位发生在测试滚动置顶之后，使 DeepSeek 首屏直接落在文献末尾，图片因 lazy loading 没加载；不是图片 404。截图 `01-deepseek/actions/continuation-1789444602660697594/screenshots/00274-S01 Word 原稿全文-0.png` 已原尺寸查看。新脚本等待 iframe load/字体完成再滚动并确认顶部；本轮须补跑完整滚动与正文定位，不能删除失败证据。
3. **测试时序**：普通两次 click 实际可能等第一次下载结束再发第二次，造成故障路由未结束即被撤销、Playwright `Route is already handled`。最新脚本改用真实 dblclick、等待故障响应发送完成并断言只发一次。需补跑受影响的下载/异常章节。原有错误记录保留。
4. **网络/核验路径**：辅助 API 原稿下载 30/180 秒超时（02/03），HTTP 已返回 200，浏览器与辅助请求出口不同。改用真实浏览器下载核对后再判定产品下载是否存在问题，不能仅扩大超时掩盖用户问题。

下一步保持首轮应用不变，完成余下 14 例双模型新转换与全操作、补足上述验收脚本阻断路径及新增遗漏入口；持续汇总但不提前修复产品。`scripts/review_web_public_images.py` 只生成/登记实际看过的图版，不自动判定通过。监测磁盘（12:06 本轮约 2.7 GB，初始可用 217 GB）；已有截图不删除。

### 12:30 接续记录（以此节为准，第一轮未完成）

- 首次转换进程 90339 已结束。28 份 `convert/result.json` 均已建立，覆盖全部 14 例 × DeepSeek / Qwen；均有真实调用、新增 Token，且本地响应复用为 0。03 双模型通过前端实际下载 37 MB Word 并核对原文件哈希后接续完成，没有重传或改用旧任务。
- 应用仍固定在发布树的 `87d075b`，不因测试发现问题而中途修改。开发树最近提交 `a0fdcc7` 之后只增加测试脚本的拖放/损坏 Word 补充路径、连续动作记录，以及续跑 trace 防覆盖；没有改应用。
- 会话 98285（PID 1315759）启动时仅包含 01 / 02 四组，使用旧脚本；01 两组和 02 Qwen 已执行其 7 章，02 DeepSeek 仍进行中。该旧脚本的 `complete.json` 只表示章已尝试，并非通过。四组均须补 `remaining_entries`、`upload_paths`，并补跑被测试时序问题及产品断点截断的路径。02 Qwen 重新转换前的真实保存收到公网 502，重转章未走完，需保留问题并验证重试退路。
- 会话 46490（PID 1332383）于 12:13:11 启动，只快照到当时已有结果：03、04、05、S01、S02、S03 双模型，以及 S04 DeepSeek（12:13:10 建立结果）。S04 Qwen 和 S05 / X01–X04 不在这批队列。该进程含强制刷新等时序修正，但不含 F05b 失败后的继续测试分支；因此原稿失败可能截断后续双页面冲突测试，需要补跑。
- 会话 76491 新启动，专测 S05、X01–X04 双模型，并发 2；使用最新脚本。与两个旧进程的稿件不重叠。不要另开进程修改这些同一任务。
- 新脚本 `reset_view` 已确保同一 URL 真正刷新；`exceptional_paths` 将失败原稿恢复单列为 F05b，记录断点后刷新继续双页面冲突，而不是直接结束整个章节。补跑用 `--repeat --chapters ...`；证据写到 continuation 子目录。`trace.zip` 也改为同一独立证据目录，避免补跑覆盖之前的 trace。
- 当前实际视觉审查登记：`01-convert-screenshots` 的 1–15 图版全部已看；`01-convert-frames` 仅 1–8（共 555 图版）；`01-deepseek-actions` 仅 1–12（共 136 图版）。其余均未看，不能当作通过。最后一批查看的是 01 DeepSeek 参考文献/搜索/摘要/文章信息的截图，未发现额外视觉问题。
- 03 Qwen 原稿侧栏下载出现两次 `Download.save_as: canceled`，需与其他入口下载成功事实分别核实；不能因首次转换下载成功就豁免侧栏路径。02 Qwen 编辑公网 502、原稿 iframe 恢复失败等均保留日志，完成全轮后统一归因修复。
- 12:26 本轮证据占用 8.1 GB，剩余约 206 GB。持续监测，不丢弃连续帧。结果页操作、被截断路径补跑和全部视觉审查远未完成，不能宣布收敛。

### 12:48 接续记录（第一轮仍未完成；不要提前修复应用）

- 重新读完最新用户要求、项目纪律和恢复记录。正式应用仍为 `87d075b`，服务 active、PID 1304807。没有修改提交材料或应用代码。开发树最新已提交 `9abb387`，新增的仅是连续帧审查工具及其 4 项自测，均通过。
- 全部 28 份首次转换已完成，初次转换进程 90339 已退出；原 01/02 操作进程 98285 也已退出。01、03、S05 双模型均已完成“当前/首次出版设置”的实际重新转换。02 双模型的重新转换仍须补跑，不能因章名出现在 complete.json 中而认定完成。
- 活跃操作队列：46490 / PID 1332383（03–S03 双模型、S04 DeepSeek，目前在 04）；76491 / PID 1346484（S05、X01–X04 双模型，目前 X01）；20265 / PID 1347565（01/02 补充，01 已完成、目前 02）；33723 / PID 1363865（S04 Qwen，单独补齐先前未纳入队列的一组）；78694 / PID 1365313（01 双模型 responsive、restore_and_recent 补跑）。启动新队列前核对进程和样例，不得同时操作同一个任务。
- 新增 `--providers` 筛选，供指定模型单独接续；不改变实际调用与验收范围。旧进程 46490 不含 upload_paths、F05b 后继续冲突测试的改进，必须在其完成后补上这些路径。当前启动的其余队列包含 upload_paths；01 双模型的真实损坏 Word、键盘选择、拖放、替换路径已执行。
- 多组平板/笔记本侧栏滚动中断：已实际查看 01 DeepSeek 的 00848–00851 截图，滚轮落在内部“所属单位”滚动区域，未作用于外侧栏。只修正验收脚本，将外侧栏全文滚动鼠标移到右侧留白，仍用真实滚轮，不直接设置滚动结果；78694 正在补验。这不是已修复的产品问题。旧进程加载的是修改前脚本，仍需补跑受阻响应式路径。
- 新发现并保留证据：03、01、S05 多处多篇文献引用链接使用整串 ID（如 `#b11%20b12%20b13`），点击找不到对应目标。原稿失败 iframe 重开不恢复仍可复现。S05 两模型原稿 o2 图片加载失败，部分 Word 下载取消；有实际 `net::ERR_NETWORK_CHANGED`，待全轮后结合请求记录区分网络与产品问题，不在中途定案或修复。
- 连续帧审查采用无损差分：每个源帧核对哈希，所有变化像素的矩形贴回前帧后须逐像素一致；每次动作改变保留整页上下文，完全相同图块共享审查。所有原帧保留，不按时间抽帧，不自动判通过。此方法已向用户说明。
- **实际视觉审查登记**：`visual-deltas/01-convert` 的 full 1–25、micro 1–63、strip 1–7 全部已打开审查并登记，覆盖 7626 个提供的连续帧事件、6470 个差异图块。`visual-review/01-convert-screenshots` 1–15 全部已看；`01-deepseek-actions` 仅 1–40 已看；`other-convert-screenshots` 本次已看 1–6，待写入登记。原 `01-convert-frames` 只实际看过 1–10，不能自动全选，该批余帧由无损差分关联审查，不虚报全图逐张打开。
- 图像生成会话 65561 正在处理 `visual-deltas/other-convert`，不代表已审查；此前 `other-convert-screenshots` 的 127 图版已全部生成，只有上述实际看过范围有效。多数结果页截图和连续帧尚未审查，本轮远未收敛。12:43 本轮占用约 15 GB，继续保留证据并监测空间。

### 13:00 接续记录（第一轮未完成）

- 应用和正式服务仍未变动。开发树最新 `c073c4a` 只修正恢复结果章节的独立测试前提：若任务已恢复原版，先核对原 XML 哈希，再通过界面实际修改并保存题名，然后验证恢复；不把正常禁用的恢复按钮当作故障。01 双模型 responsive 已完整通过；01 Qwen 恢复补验会话 65452 已结束、无问题记录。
- 01/02 补验会话 20265 已结束。02 Qwen 补跑的主要剩余问题为原稿失败后不恢复和多引用链接失效；02 DeepSeek 另有保存未成功、下载等待超时及一次 `ERR_NETWORK_CHANGED`，均保留证据，尚未统一归因。新会话 61265 / PID 1381421 正在补 02 双模型 responsive、reconvert；不得并发操作同一任务。
- S04 Qwen 单独会话 33723 已结束，九章均尝试；原稿 o2 图像加载失败、两次 Word 下载取消导致部分路径未走完。新会话 64563 只补此组 read_content、checks_and_downloads。此前 responsive、editing、restore、实际双次 reconvert、remaining_entries、upload_paths 均有通过记录。
- 原队列 46490 / PID 1332383 仍运行，已走完 04 DeepSeek 并进入后续样例，04 Qwen 重转仍在进行；76491 / PID 1346484 在 X01 收尾；44116 / PID 1370220 已补 03 双模型 read/checks/conflict/responsive/upload，正进入 S05。旧队列的缺失 upload_paths、冲突和响应式受阻路径仍需逐例补齐。不要因为 complete.json 有章名就认定覆盖完成。
- **首次转换全部操作截图已实际审查**：01 图版 1–15；其余 26 组 `other-convert-screenshots` 图版 1–127，1276 张截图、505 个精确不同图均已登记。01 首次转换连续帧审查保持上一节已完成状态。`01-deepseek-actions` 本次已打开 41–46，待登记；其余动作截图和连续帧仍未完成。
- 图像生成会话 65561 继续处理其余首次转换连续帧；新增 83207 处理 01 双模型所有结果页动作连续帧。两者都只生成无损差分审查材料，不自动登记审查或通过。13:00 本轮约 23 GB，全部证据保留。

### 13:28 接续记录（首轮尚未完成）

- 正式应用仍固定为 `87d075b`，服务正常运行；没有提前修复应用，也没有修改已提交材料。开发树 `89b5d7e` 增加验收进度核算和独立尺寸接续；验收工具的 7 项自测通过。AGENTS.md 的用户修改不纳入提交。
- 46490 / PID 1332383 队列推进到 05 的真实重新转换，后续为 S01–S03 双模型、S04 DeepSeek。76491 / PID 1346484 已完成 X02 并进入 X03 双模型。旧队列仍缺 upload_paths、原稿故障后继续版本冲突等补充路径，必须在该稿件退出旧队列后补验。
- 02 双模型的两次实际重转、响应式补验均已完成；02 DeepSeek 编辑与保存成果下载补验通过，既往失败记录保留。03 双模型的原稿/下载/异常/响应式/上传补验已结束。04 Qwen 的补验 22382、X01 双模型的补验 21042 已结束；69527 / PID 1407805 补验 04 DeepSeek 和 S05 DeepSeek 响应式，04 已通过，S05 仍有断点。不得并发修改同一任务。
- 新的明确视觉问题：04 DeepSeek 的 `actions/continuation-1789448921996149675/screenshots/00218-failure.png` 中，侧栏标题为“文章信息”，内容却是原稿 iframe，无法编辑；已打开原尺寸确认。补跑成功不能抹去该故障。另有 X01 预览 iframe 的 contentDocument 为 null、S05 不同面板异步失败等，先保留记录，整轮后统一归因。
- 新版 responsive 将六个尺寸独立执行；某一尺寸失败后保留证据并真实刷新，继续其他尺寸，避免一个产品断点使手机部分未执行。旧进程不会自动获得此改动，补验须核对其实际动作。
- 实际视觉审查：`01-deepseek-actions` 的 1–136 图版已全部打开并登记；范围仅限 `continuation-1789444602660697594`，不代表 01 的其他续跑。首次转换操作截图沿用前节已全部完成状态。`other-convert` 连续帧差分图版仅 full 1–12 已实际看过，micro / strip 尚未看；`01-convert` 三类差分仍为全部已看。
- 图像生成已完成：`visual-deltas/other-convert` 共 102238 帧记录、28228 个差分图块（micro 262 / strip 78 / full 211 图版）；`visual-deltas/01-actions` 共 43797 帧记录（micro 90 / strip 33 / full 1489 图版），后者均未审查。`visual-review/01-remaining-actions` 共 609 图版尚未看，只排除了与已审查图像哈希完全相同的副本，不抽帧，不自动批准。
- `scripts.audit_web_public_round` 汇总逐例实际新调用、章节/子步骤问题、真正审查过的图像哈希；早期没有 chapter-end 的原记录单列，不当成全章通过。新增可见控件与实际操作记录比对仅用于查漏；旧记录器未采集事件的控件须回查原步骤，不能直接报成产品问题。13:28 磁盘剩余约 166 GB，所有证据保留。

接续顺序：核对运行队列 → 补足各例实际未执行的路径 → 完成全部视觉审查 → 汇总整轮问题并统一修复 → 新发布树与新缓存上线 → 全量回归、14 例双模型全新调用和全部操作/视觉重跑。当前离全轮完成尚有大量工作，不能报告通过或收敛。

### 13:41 接续记录（第一轮未完成，以此节修正旧队列状态）

- 正式应用仍是 `87d075b`，服务 active；开发树最新 `6f4c61b` 仅更新测试，10 项验收工具自测通过。没有修改提交材料、原任务或用户 AGENTS.md。
- 旧队列 46490、76491 均已退出，原因是附加浏览器 trace ZIP 导出报 `file data stream has unexpected number of bytes`，连带取消另一并发任务。该问题不等于网页失败；原截图、动作和失败记录全部保留。新版将附加记录导出异常单独存档并关闭对应浏览器，不再取消其他稿件。
- 接续队列 43576 / PID 1422063 正执行 S01–S03 双模型及 S04 DeepSeek 的九章，S01 已进入真实重转收尾。16801 / PID 1425423 正执行 X04 双模型九章，已到编辑。S04 Qwen 已有九章，不会被该队列重复操作。
- 05 双模型异常/六尺寸/上传补验 5820 已结束；X03 双模型六尺寸/上传/引用补验 8824 已结束。01–04 / S05 的检查下载及引用补验 76231 已结束，其中 04 DeepSeek 在进入任务时出现 `ERR_NETWORK_CHANGED`，未执行该批操作，现由 39939 单独补齐。
- X01 Qwen 补读全文已完成、无新断点；X01 DeepSeek 原稿 o14 图片失败仍导致此前正文定位和侧栏下载漏验。现将图片检查单列，失败照记，继续实际定位和下载；23454 补 X01 DeepSeek 原稿/检查下载/引用，95908 补 X01 Qwen 检查下载/引用。不得再并发操作这些同一任务。
- 内部引用现一律先实际点击再检查目标，坏链接也不跳过；剪贴板拒绝后实际点击并全选任务链接。预览加载完成还须确认返回的确为当前任务的非空文章，不能把空白 iframe 误报成“没有引用”。S04 Qwen、X02 两模型仍须补这两项；X03 的引用已补，手动链接全选待补。
- 实际视觉审查：`other-convert` 连续帧差分 full 1–110 已打开检查，micro / strip 仍未看；首次转换操作截图及 01 转换差分沿用此前全部已看记录。`01-actions` 和 `01-remaining-actions` 两组仍未审查。生成文件和章节完成均不代表通过。当前磁盘剩余约 160 GB，证据不删减。

### 14:01 接续记录（第一轮未完成）

- 正式服务仍 active，应用仍为 `87d075b`，没有修改已提交材料。开发树 `dd2fd75` 仅移除测试恢复时多余的网络状态重设，11 项验收工具自测通过。只读对照实验确认：同一浏览器上下文反复设置已经为假的 offline 状态，可以中断在途请求；另一上下文的对照未复现。该结论不能解释全部 `ERR_NETWORK_CHANGED`，原错误不删除。实验记录为本轮 `network-diagnostic.md`。
- X04 双模型九章及两种出版设置的真实重转均已执行，16801 已结束。仍有文件说明中的 XML / 成果下载超时、失败原稿重开不恢复及多引用跳转失效。S01 双模型也已走完九章；43576 / PID 1422063 正在 S02 真实重转，后续为 S03 双模型、S04 DeepSeek。不得并发修改该队列的任务。
- X01 DeepSeek 原稿补验已完成，正文逐段点击、图片检查和原稿下载有独立记录，图片断点不再阻断后续操作。04 DeepSeek、S04 Qwen、X02 / X03 / 05 双模型的检查下载和实际引用点击补验已结束；保留 04 / X03 DeepSeek Word 下载取消、X02 Qwen 检查页 XML 下载超时等问题。S04 Qwen 此次剪贴板拒绝后手动选中完整链接已通过。
- X02 DeepSeek `continuation-1789451345247024475` 的问题名沿用了 D05b，但实际点击和 Ctrl+A 已执行且选区断言通过，出错的是随后打开文件说明时出现载入失败；归因时必须读实际事件和截图，不能误报“手动复制失效”。S05 DeepSeek 响应式补验 70162 已结束，六种尺寸独立尝试，但笔记本原稿载入仍失败，不能当作全通过。
- 实际视觉审查：`other-convert` 的 full 1–211 已全部打开并登记，micro 1–49 已打开并登记（40–49 登记进程 35482 待确认完成），strip 尚未看。该批小变化主要为进度转圈及少量上传进度，不豁免剩余图版。`01-actions` / `01-remaining-actions` 仍未审查。新增生成队列 20024 处理已结束的 02–05 双模型动作连续帧，目录 `visual-deltas/02-05-actions`；生成不代表已看。
- 最新进度核算：28 份首次新调用，607276 个连续帧事件、54225 张操作截图；实际登记覆盖 93450 帧、3115 张截图（此数早于 micro 30–49 登记，且采集仍继续）。读证据无错误。磁盘剩余约 152 GB，所有原始证据保留。

仍按固定顺序完成首轮操作与全部视觉审查，再统一修复应用、发布新版本、重新执行全轮。已尝试、补跑成功、进程退出均不能替代完整验收。

### 14:27 接续记录（第一轮未完成，网络配置未改）

- 正式服务仍为 `87d075b`、PID 1304807，active，重启次数为 0。开发树 `ae83150` 只增加小图块原尺寸审查排版及自测；12 项验收工具自测通过。用户 AGENTS.md 和已提交材料不动。
- 43576 队列已退出。S01 / S02 双模型九章均有实际执行；S02 Qwen 的 remaining_entries 中断由 19392 补齐，实际点击 93 条引用，其中一条多目标引用失效。S03 双模型完成前五章的尝试，但 DeepSeek 手机横屏原稿 / 文章面板失败，Qwen 文件说明成果下载超时；恢复、重转和后续入口未走完。S04 DeepSeek 进入任务即超时，九章仍未执行。
- 接续 8450（S03 双模型后四章）和 90966（S04 DeepSeek 九章）均在初始预览等待 90 秒后退出，没有实际执行这些章。保留独立 continuation 证据，不记为覆盖。当前无操作队列运行；20024 仍生成 02–05 的动作连续帧审查材料，不代表已审查。
- 本机网络存在独立干扰证据：NetworkManager 中 `enx1afdc44df54a` 每 45 秒申请 IPv4 失败并重新激活；14:22:30、14:23:15、14:24:00 的事件时间分别为 1789453350.649、1789453395.650、1789453440.650，而三个测试页面分别在其后约 3–8 毫秒同时出现 `ERR_NETWORK_CHANGED`。只读 `ip monitor` 也记录此接口的 IPv6 地址和路由删除。该关联支持本机环境影响，但尚未通过停止该接口作因果对照，不能解释或豁免全部网页问题。已向用户询问是否允许临时断开这一个接口、测试后恢复；尚未收到批准，任何系统网络配置均未改变。
- X02 DeepSeek 的最新 D05b 名称沿用问题已核对：手动全选链接成功，随后文件说明载入失败；原 `actions/progress.json` 中 D07 / D08 已分别实际下载 XML / 成果并完成文件校验，不需为补齐路径盲目重跑。
- 实际视觉审查：`other-convert` full 1–211 全部已看；micro 1–89 原图版已看，其余小图块通过 `tiny/tiny-00001.png` 至 `00036.png` 全部以原尺寸实际审查并登记，所有 micro 图块已覆盖。strip 1–50 已实际打开，45–50 的登记进程 69046 待确认；51–78 未看。`01-actions`、`01-remaining-actions` 和正在生成的 `02-05-actions` 仍未审查。14:23 磁盘剩余约 144 GB，原证据不删除。

恢复执行时先核对网络授权与诊断，不改正式服务来掩盖本机网络问题；继续审查原有证据。网络可用后补 S03 后四章、S04 DeepSeek 九章及实际受阻的尺寸 / 面板路径。全部操作与视觉完成后才进入首轮统一修复。

### 14:43 接续记录（用户已批准临时断开单一网卡，测试后必须恢复）

- 用户明确批准临时断开 `enx1afdc44df54a`，测试后恢复。检查时设备已经 disconnected，但设备自动连接为 yes；`nmcli device disconnect` 因设备未激活而未执行。随后仅执行 `sudo -n nmcli device set enx1afdc44df54a autoconnect no`，暂时阻止此设备再次自动申请地址。连接配置 `3114f0c3-a6f8-3940-abe9-37b86b5459ef` 的 `connection.autoconnect=yes` 未改；eno2 默认路由、网站服务、隧道均未改，正式服务仍为 PID 1304807、NRestarts=0。
- **测试结束必须恢复**：执行 `sudo -n nmcli device set enx1afdc44df54a autoconnect yes`，核对设备自动连接恢复及原连接配置仍在。如需恢复原连接尝试，只激活该 UUID，不触碰主网卡。不得因 compact 遗忘此义务，也不得把暂时禁用变成永久配置。
- 网络授权已到，不再先扩大测试脚本的初始等待时间；沿用现有超时和断言补 S03 两模型后四章、S04 DeepSeek 九章，用实际请求与操作结果验证临时断开后的影响。此前失败全部保留，不改判。
- 实际视觉审查更新：`other-convert` full 1–211、所有 micro（直接图版 1–89 及 tiny 1–36）、strip 1–78 均已打开并登记，28228 个差分图块全部覆盖。因此 28 份首次转换的操作截图和连续帧差分均已审查。`01-actions` 仅 full 1–48 已打开并登记（192 / 15430 图块），其他类别和后续图版未看；结果页整体视觉审查尚远未完成。
- 新的待核实内容：01 Qwen 表格脚注有重复 Continued；S04 DeepSeek 文首夹有 Comment 和 #。须对照原 Word 与转换 XML 判定，不能只凭画面认定错误或先行修改。应用仍固定 `87d075b`，提交材料不动。

### 15:08 接续记录（第一轮未完成，临时网卡恢复义务仍在）

- 应用仍固定 `87d075b`，没有提前修复。`enx1afdc44df54a` 当前 disconnected、设备 autoconnect=no；测试后按上一节恢复为 yes。主网卡、隧道和服务不改。
- 临时隔离后，沿用原等待时间，S03 两模型后四章、S04 DeepSeek 原九章已执行完；S02 / S03 / S05 DeepSeek 六尺寸独立补验均无新问题记录。S03 DeepSeek 引用 `#b9%20b10`、S04 DeepSeek 失败原稿重开不恢复仍实际重现，不归为网络干扰。前述 S03 / S04 补验事件未再记录 ERR_NETWORK_CHANGED。
- 已逐份汇总 `actions/**/reconversions.json`：56 条、56 个不同任务，均有真实调用和 Token，cache_hits / reused_responses 均为 0；加上 28 次首次转换，共 84 次。九个原章节全例均有执行记录，但章节尝试不等于所有子步骤通过，旧失败仍待整轮归因。
- 新补横向宽表阅读：六尺寸中逐列横向滚动、逐屏纵向读完并返回左端；原稿另检查键盘滚动。26 组的原稿补验队列 53480 已结束；S04 两组的完整横向补验 13046 已结束。早一批脚本把预览当成原稿的独立表格容器，预览统计为 0 不计覆盖；已改为实际预览文档滚动，01–03 两模型补验已结束，剩下 20 组由 21097 继续，不能省略这项补验。
- 实际视觉登记：`01-actions` full 1–108 已打开并登记（432 / 15430 图块）；其他类别未看。图版 49–102 的全文原稿/定位/下载及 103–108 的题名、作者和 ORCID 编辑未见新增持续性布局破坏。01 Qwen 表格重复 Continued 候选仍保留。全部首次转换视觉已完成，结果页视觉仍远未完成。
- 15:03 附近核算：702758 帧事件、61569 张操作截图，实际登记覆盖 190288 帧、3202 张截图，无证据读取错误。采集仍在继续，不能以生成量代替审查量；磁盘剩余约 131 GB。
- 验收工具新增可选空间分块：仅沿完全不变的空带拆开多个相距较远的变化区域，保留周边像素；每帧必须由前帧加全部图块逐像素重建一致，前帧和所有图块均实际审查后才计覆盖。旧原图、旧审查材料均保留，生成不自动批准。17 项验收工具测试通过（14.81 秒）；首次运行遗漏专用浏览器路径造成三项启动失败，正确指定既有浏览器目录后全部通过，未安装或修改系统浏览器。
- 96252 正生成 `visual-deltas/01-actions-spatial`，20024 继续生成 `02-05-actions`；均不代表已看。开发树变动仅为验收脚本和本记录，用户 AGENTS.md 不纳入 Git 提交。

接续仍先查各进程与实际子步骤，再完成全部视觉和整轮问题汇总，随后才修改产品和重跑新一轮。网卡临时隔离不能遗忘，提交材料不能触碰。

### 15:24 接续记录（网卡已恢复；首轮结果页视觉仍未完成）

- **临时网卡已恢复**：确认所有公网操作队列退出后，15:24:33 执行并核验 `nmcli device set enx1afdc44df54a autoconnect yes`。设备 GENERAL.AUTOCONNECT=yes、原 UUID 的 connection.autoconnect=yes，设备已重新开始申请地址；默认路由仍为 eno2，网站 PID 1304807、active、NRestarts=0。上一节的恢复义务已履行。后续若再需隔离作公网测试，仍只按用户授权暂时处理这一个设备、测试后恢复，不改主网卡或隧道。
- 21097 全部 20 组预览宽表补验已结束。手机目录浮层展开导致滚轮打在目录上，已原尺寸看过 01 DeepSeek `continuation-1789455205268990650/screenshots/00051-failure.png`、00054、00057 确认。测试脚本现先实际点击“收起目录”，90584 对全部 28 组补验手机宽表，其中 26 组通过。
- 余下 X01 两组并非表格滚动卡死：读完当前表格后，脚本继续按整篇文章的最大宽度向右滚到该表格之外。已看 `X01-deepseek/actions/continuation-1789456442216322940/screenshots/00014-H02 手机 转换预览 宽表 1 列屏2 行屏0.png` 和 00015-failure.png。测试边界改为当前表格右端，18130 单独补两组均 issues=[]，对应新 continuation 为 1789456929896419794 / 1789456929911376338。所有失败原证据保留，不改判为原网页已修复。
- 横向阅读所需的六尺寸、两种阅读区均已有逐例实际路径；仍需将所有具体事件、截图与记录合并检查，不能仅凭章节或进程退出宣布通过。没有新的公网操作进程在运行。
- 内容来源补核实：01 Qwen 两个 Continued 分别在 Word 的同一 `mc:AlternateContent` 下同时存在于 Choice / Fallback 文本框，两套兼容表示进入输出，造成重复；不是简单的模型凭空编字。S04 DeepSeek 原 Word 作者行有 `#`，输出为 `<author-comment><p>#</p></author-comment>`，预览样式 `webapp/vendor/jats/jats-html.xsl` 的 metadata 模板自动加 `Comment` 标签。整轮汇总时据此确定处理层次，不盲目删原文符号。
- 实际视觉：`01-actions` full 1–120 均已打开并登记（480 个图块），109–120 为作者/单位/通讯字段变更及还原，没有新增持续性布局破坏。full 121 以后、micro / strip 未审查。新 `01-actions-spatial`（96252）和 `01-actions-png-spatial`（93647）仅生成，不能登记已看；后者单独处理全部操作 PNG，与连续帧分开计数。02–05 原连续帧生成 20024 仍在运行。
- 验收工具 20 项测试通过，26.86 秒；覆盖精确多区域重建、每区及前帧必审、PNG 事件完整保留、实际浏览器宽表/浮层/其他宽对象边界。正式应用未改。最新开发提交 `c9430d4` 后还有本节对应验收工具更新待提交，AGENTS.md 仍只保留用户修改。

下一步集中完成全部结果页图像审查和具体功能路径核对，统一归因后再修复、重新发布并执行下一轮。不能以测试工具自测通过冒充 Web 全量验收完成。

### 15:44 接续记录（第一轮未完成；网卡恢复已履行）

- 正式应用仍为 `87d075b`，没有提前修复或修改提交材料。设备自动连接已在 15:24 恢复，不按旧小节重复禁用。
- `01-actions` full 1–252 全部实际打开并登记，覆盖 1008 / 15430 个图块；full 253 以后和全部 micro / strip 未看。首次转换视觉全部已审查，其他结果页视觉仍未完成。
- 新补核实的交互问题：上一下载成功提示可与下一次下载失败横幅短时并存，已通过事件时间和 4500 毫秒提示计时确认，并非同一次请求同时成功与失败；空题名保存被拒绝后侧栏滚到表单底部，未定位到题名输入框。证据路径及旧手机文件说明失败画面登记在本轮 `visual-observations.md`。均留待整轮汇总处理。
- `01-actions-spatial` 生成已结束：54115 帧、21397 图块、full 2321 / micro 112 / strip 60 图版，均未审查。`01-actions-png-spatial` 也已结束：6226 个 PNG 事件、3824 图块、full 657 / micro 9 / strip 25，均未审查。生成量不是审查量，也未证明增加空间分块能减少整体工作量；继续使用已有材料实际检查，不反复生成替代版本。
- 20024 仍在生成 `02-05-actions`（84604 图块已建立、图版尚在生成）；15721 正更新实际验收进度核算。无公网操作队列运行。15:43 可用磁盘约 114 GB，原始证据全部保留。

继续核对具体子步骤和剩余视觉，完成第一轮后才统一分析修复；章节已尝试、脚本退出和图像已生成均不代表通过。

### 16:00 接续记录（第一轮未完成，网卡已恢复）

- 正式应用仍为 `87d075b`、PID 1304807、active、NRestarts=0；未提前修复应用，未改提交材料。开发树只维护验收证据与本记录，用户 AGENTS.md 不纳入提交。
- X03 Qwen 原先真正未执行的 B02–B10 已补：`actions/continuation-1789458660437162380/progress.json` 记录 70 个动作、issues=[]，包括关闭草稿/继续编辑、空题名拒绝、刷新、预览故障 XML/下载退路、失败态重新上传与重转入口、61 条引用点击。重转入口只打开并取消，没有新增转换调用。原失败保留，不改判。
- 此项补验在 15:51 再次按授权临时隔离单一网卡，shell EXIT trap 在进程结束时恢复；15:53 及其后只读核对设备 GENERAL.AUTOCONNECT=yes，原连接配置自动连接 yes，默认路由仍为 eno2。没有公网操作队列运行，恢复义务已履行。
- 逐例核查旧脚本和子步骤：旧 read_content 直接执行图片断言和原稿全文滚动，未分别记录 R12/S01/S01b；28 组均有其后 S02 的实际逐段定位记录。A00（已恢复任务的测试前提）与 B01（可见提示条）为条件分支。不能机械地把旧步骤编号缺失当作没执行，也不能据此将真实失败当作通过。动态字段、目录和下载仍继续核查。
- 实际视觉：`01-actions` full 1–360 已全部打开并登记，1440 / 15430 个图块；其余 full 361 以后、micro / strip 未看。新确认预览作者地址来自 contrib/address，所属单位来自 article-meta/aff；full 339 的 P8968 及后续帧实际显示完整单位，并非编辑后字段丢失。空题名错误定位问题仍保留。
- 最近一次整体核算（早于本批登记与 X03 补验）：728934 帧事件、62707 张操作截图；实际登记覆盖 207725 帧、3205 张截图，证据读取无错误。首次转换视觉已全部审查，结果页远未审完，不能报告全量通过。20024 仍生成 02–05 连续帧图版，生成不代表已审查；16 点前磁盘剩余约 107 GB。

下一步仍完成结果页全部证据审查与逐例路径核对，结束首轮后统一分析修复，再发布新版本并重跑全轮。不得因已收集大量证据或补验成功直接跳入修复或宣布收敛。

### 16:08 补充验收计划：关键字段修改必须进入交付文件

逐项复核 `editing()`、异常、恢复和重转章节后确认：原 E02/E03/E10 逐个操作了姓名、ORCID、通讯作者、单位关联、单位正文和通讯邮箱等控件，但保存前将这些内容恢复，真实保存只覆盖题名和 DOI。因此不能将控件操作完成等同于这些修改已进入 XML。

本轮新增 `metadata_persistence`，逐例双模型通过公网界面修改全部实际存在的文章字段、调整同组作者顺序、填写自定义出版信息和分别保存已配置期刊。每次保存核对刷新后的字段、预览、XML 和下载文件，确认正文与文献不被连带改写、模型用量不增加。没有作者、单位或通讯信息的稿件明确记录不适用，不虚构内容。保留修改前数据及 XML；补验结束通过现有恢复按钮还原该测试任务的自动转换结果，不修改非测试任务。

这只是补足首轮验收，不改线上产品、不跳过旧失败、不重新调用模型。测试工具先自测并提交 Git；需要公网操作时仍只按授权临时断开指定网卡，进程退出即恢复。结果页视觉继续审查：`01-actions` full 1–384 已实际打开并登记，其余尚未完成。

补验判定按现有产品功能区分：`webapp/render.py` 的 `_strip_diagnostic_front` 明确移除期刊诊断字段，因此刊名、ISSN 等在出版面板、XML 和下载中核对，不要求其出现在阅读预览。首个 01 Qwen 补验脚本误将刊名也作为预览文本断言，保留其失败证据，修正验收器后补跑；不能据此改造产品或把这批失败删掉。文章姓名、单位和题名仍须在阅读预览中实际核对。

### 16:28 接续记录（首轮未完成，网卡仍临时隔离）

- 正式应用仍为 `87d075b`、PID 1304807、active、NRestarts=0；未改应用、隧道或提交材料。开发树 `598b03f` / `7e07789` / `ad4be33` 只补真实字段保存验收及判定修正。新增元数据工具与已有视觉、宽表工具合计 31 项自测通过；28 份当前 XML 的独立字段读取与现有编辑器只读比对通过，不代表公网保存和交付已经通过。
- 01 Qwen 元数据补验会话 74917 / Python PID 1659038 仍运行，证据 `continuation-1789459985110600153`。此进程加载修正前的刊名预览断言，因此前半批虽已真实保存，未走到后续下载。保留这些脚本误判；后半批又遇到保存确认与页面载入超时，不能将其一并归为断言错误。所有预设逐一尝试后，脚本会通过界面恢复测试任务的自动结果，须核对 M99 与 `metadata-restored.json`。
- 22035 / shell PID 1664152 等待前一 shell PID 1659018 完全退出，再以 `ad4be33` 对全部 28 组运行 `metadata_persistence`，并发 2。不得另外操作同一个任务；后续须查实际开始情况、各字段保存和下载，以及最终恢复。
- 本批于 16:13 再次按授权临时设置指定设备 autoconnect=no，16:25 核对设备 disconnected、默认路由仍为 eno2；原连接配置未改。两个执行 shell 均设 EXIT 恢复，后一批须在前一批恢复后再次临时隔离。**全部公网测试结束后仍须核对设备 autoconnect=yes**，不能仅依赖 trap 而不检查。
- 新的只读网络证据：本地 `/api/journals` 200、约 0.002 秒；公网同接口 200、约 8.93 秒。隧道在 16:20–16:24 多次记录 edge connection closed / http2 stream closed 并自行重连；同期浏览器记录保存确认请求取消及页面载入超时。NetworkManager 自本批隔离后未记录该设备再次申请地址。此批不能沿用先前网卡原因，也没有据此重启隧道。
- 实际视觉：`01-actions` full 1–456 已打开并登记；457–468 已打开，登记进程待确认。近期画面为文内引用定位、正文和图表阅读，重复 Continued 已在原问题中保留；单帧预绘制留白后有内容帧，不据此增加持续白屏结论。其他类别及多数结果页材料未审查，首轮不能宣告通过。
- 02–05 连续帧审查材料生成 20024 已完成：230623 帧事件、84604 图块，full 11659 / micro 348 / strip 199 图版，均未实际审查。生成量不计验收完成。当前磁盘约 102 GB 可用，原证据保留；后续持续监测空间。

继续顺序不变：补齐实际未执行的功能路径与全部视觉审查，汇总整轮问题，再统一修复、重新发布并重跑全轮。章节尝试、进程退出和补跑局部成功不等同于全覆盖通过。

### 错误输入分支补充计划

现有公网脚本只实际提交过空题名，DOI、ISSN、ORCID、邮箱等错误处理尚不能以后台单元测试替代。因此增加 `validation_inputs`：逐例通过实际表单提交空值、格式错误、校验位错误及长度边界，检查拒绝响应、明确提示、输入保留、放弃修改和原 XML/版本/用量不变；最后实际下载 XML。每个实际作者的 ORCID 和每个实际通讯邮箱均操作；不存在的字段记为不适用，不伪造输入控件。每个独立阶段失败后保留证据，继续其他阶段，不把章节结束等同通过。

此项不增加模型调用，不修改应用。必须等待相同测试任务的元数据保存补验退出后执行，不与现有队列交叉修改。长度与格式取现有 `webapp/editor.py` 的界面可达校验边界，不扩展成接口篡改测试或无限输入排列。

### 16:52 接续记录（第一轮仍未完成）

- 元数据旧会话 74917 已退出，最后恢复步骤也因请求失败未完成；不将旧失败改判。新会话 22035 已接续全部 28 组，01 两模型现各完成 223 个动作、issues=[]，包括实际字段保存、作者排序、自定义出版信息、21 个期刊预设及每次保存后的刷新、XML 和成果包核对。两份 `metadata-restored.json` 已核对，测试任务恢复到原自动结果，旧 01 Qwen 未恢复状态已消除。新证据分别为 `01-dashscope/actions/continuation-1789461150011164256`、`01-deepseek/actions/continuation-1789461150026298239`。22035 正执行 02 两模型，不重复操作这些任务。
- 错误输入工具及相关自测共 36 项通过；以 28 份 XML 生成的 816 项只读内存校验无不一致。这两项不是公网界面通过。已另起会话 15779，只对元数据补验已结束的 01 两模型执行 `validation_inputs`，不与 02 及后续元数据队列交叉修改。其他 26 组错误输入公网测试尚未开始。
- 实际视觉登记：`01-actions` full 1–510、`02-05-actions` full 1–20 已打开并登记，其余类别未完成。`visual-review/failure-states-1639` 的 95 张图版全部实际打开并登记，涉及 492 张失败/中断截图、380 个不同图像；这只覆盖选中的失败画面，不豁免正常操作和其余连续帧。S03 DeepSeek 原 `actions/screenshots/00990-failure.png` 已原尺寸查看，文章信息面板内误显示原稿，与早先 04 DeepSeek 的异步面板错配同类。详细证据补入本轮 `visual-observations.md`。
- 指定网卡当前仍 disconnected、设备 autoconnect=no，原连接配置未改，默认路由仍为 eno2。22035 的 EXIT 恢复仍在；15779 未另设提前恢复，避免中断另一队列。所有公网队列结束后必须显式核对并恢复设备 autoconnect=yes，不能只依赖 trap。网站和隧道不改。
- 应用仍固定 `87d075b`，无中途产品修复；开发提交 `7ce3ce5` 仅为验收工具，AGENTS.md 用户修改保留，已提交材料不动。继续补齐功能与全部视觉，不能把两组局部通过说成整轮收敛。

### 17:07 接续记录：只读核对的网络路径修正

- 01 错误输入会话 15779 已结束。两组最终 XML 下载及版本不变检查通过，均有 `validation-inputs-restored.json`；但各有 5 条中间核对超时，不能算整章通过。Qwen 为 N19–N23，DeepSeek 为 N18–N22；错误提交已被拒绝的画面与后续只读超时分开看，仍须补足完整流程。
- 02 元数据补验两组已结束，各有 9 条问题记录，涉及 JEEMS 及后续若干预设的只读核对超时，M99 的核对也超时，尚无恢复凭据。22035 已进入 03 两模型；17:06 对其 Python PID 1684530 发送一次 SIGINT，要求停止旧核对队列并执行 finally 恢复，等待进程结束后才能用新工具接续。03 DeepSeek 当时已保存文章字段，必须核对其恢复结果；不能假定中断未产生编辑。
- 17:03–17:04 只读对照（执行会话 96227）：同一浏览器页面并发访问公网 `/api/journals`、`/api/result/a65dcaab768f4f56`、`/api/workbench/a65dcaab768f4f56`。独立 APIRequestContext 三次均 20 秒超时；浏览器 fetch 分别 1.653 / 1.684 / 1.691 秒返回 200，正文长度 2837 / 1061621 / 28281 字节。因此只读核对改用浏览器网络栈，仍走原公网地址、禁用浏览器缓存、保留原 60 秒上限，不放松结果断言、不改保存与下载的真实点击。这不能解释此前所有网页失败，更不能抹去其证据。
- 修正验收记录器的嵌套动作归属：内层字段填写结束后恢复外层阶段名，避免外层核对超时被记为“填写失败”。两项修正及字段/视觉相关自测共 52 项通过。正在运行的旧进程不会自动加载这些修正；应用代码、服务和隧道均未修改。
- 实际视觉：`01-actions` full 1–590、`02-05-actions` full 1–44 均已打开并登记。新增范围为正文、图表、文献及目录定位，未确认新的持续性布局问题；其余图版远未审完。X01 原 Word 的 `word/document.xml` 已确认含字面 `<ref>`、`<element-citation>`、`<person-group>` 等文字，不能把预览同样显示这些文字直接定为前端注入错误。
- 指定设备仍临时隔离。等待 22035 退出时先核对 EXIT 恢复；若继续公网补验，再按原授权仅隔离该设备。所有公网操作结束必须恢复并确认设备及原配置 autoconnect=yes，默认路由仍为 eno2。磁盘约 99 GB 可用，原证据保留。

接续：确认旧队列退出及 02/03 测试任务恢复情况 → 新工具补足 02–X04 元数据与全部错误输入 → 继续全部视觉审查 → 整轮汇总、统一修复、重新发布及全轮重跑。当前不能宣告收敛。

### 17:20 接续记录：辅助请求修正后继续首轮

- 旧会话 22035 以 130 退出；浏览器先于 finally 恢复关闭，不能声称中断时清理成功。其 shell 的网卡恢复在 17:07 已显式核验。17:08 新会话 7009 再次按原授权隔离同一设备，以浏览器网络栈执行 02–X04 双模型的测试结果恢复、元数据保存及错误输入，任务内顺序运行、不同任务并发 4。不得另起测试修改这些任务。
- 03 两组现已通过实际恢复按钮消除旧中断留下的修改。新 `metadata-before.json` 与各自首次转换 `convert/result.json` 的版本完全一致：DeepSeek 为 `888a4216…2829758`，Qwen 为 `8759ef98…a172f5`。恢复后的菜单被应用正常关闭，旧辅助步骤没有重新展开便点下载，造成该辅助章节失败；这不是恢复失败。验收工具 `af6c828` 已补重新展开菜单，当前队列不因这项工具错误再次中断，最终仍须核对 M99。恢复辅助工具及字段、视觉相关自测 53 项通过（0.21 秒），包括恢复前后菜单状态、版本和用量不变断言。
- 01 错误输入新会话 65567 已正常结束。Qwen `continuation-1789463304360636118`、DeepSeek `continuation-1789463304375750617` 各 54 个动作、issues=[]，分别用时 227.64 / 229.91 秒；两份 `validation-inputs-restored.json` 已读取，最终 XML 下载及版本不变核对通过。旧超时证据不删除，不能将这两组通过扩大为全例通过。
- 02、03 当前元数据保存已执行到多项期刊预设的刷新、XML 和成果下载；尚未完成全部章节。03 目前仅有上述恢复辅助菜单错误，02 未记录新问题。后续 04–X04 仍在同一队列中等待。
- 实际视觉登记推进至 `01-actions` full 1–614、micro 1–12；`02-05-actions` full 1–68。新增图版为讨论、文献定位、图表、原稿并排阅读及载入图标变化，未确认新的持续性布局问题。02 原稿中邮箱保护占位在相邻图版 66 恢复为原邮箱，不能据前一帧另报持续错误。其余类别与多数结果页证据未审完。
- 17:14 核对设备 disconnected、autoconnect=no，默认路由仍为 eno2；所有公网队列结束后须恢复并显式核验设备与原连接配置 autoconnect=yes。当前仅 7009 仍运行，65567 不再运行。正式应用和已提交材料不动，磁盘约 98 GB 可用。

首轮尚未结束，继续完成未验功能和视觉，之后才统一分析修复、发布并完整重测。
