# Web 公网上线与 14 例逐项验收

## 当前授权与恢复入口

2026-09-15 用户批准替换正式网站，并要求上线后从公网前端对全部 14 例执行全新调用、逐例全功能操作及密集连续截图检查。每轮先完成全部测试，再汇总分析、统一修复；修复后重新上线并重跑全轮，直至没有新问题。此指令替代上一轮“不部署正式站点”的限制。每次压缩上下文后先读用户最新指令、AGENTS.md、本文件和本轮进行中记录。

目标仍是可靠地完成 Word → JATS XML、查看对照、有效的关键字段修改和成果下载，不扩展为出版管理系统，不重做已提交材料，不增加形式化核对。用户已有 AGENTS.md 不提交、不覆盖。不启用子代理。

## 上线与回滚

- 待上线应用基线：`87d075b`，原开发树 `/home/denggf/word2jats-web-next`。
- 原正式源树：`/home/denggf/学术期刊结构化技术创新大赛`，`bbf05b1`；保持其代码和已提交材料不变。
- 正式入口：`https://word2jats.jianglab.work`；systemd `word2jats-web.service`、端口 8504。保留现有隧道与域名。
- 使用单独的只含本轮固定版本的发布工作树，避免开发中修改静态文件直接影响正式用户。服务覆盖配置指定发布代码和原正式运行数据目录，原任务链接继续可用。
- 切换前记录既有任务状态、当前 XML 哈希、计量和服务配置；有转换中任务则待其完成。建立 Git 恢复标签；服务覆盖配置只新增本轮文件。删除本轮覆盖配置、daemon-reload、重启原服务即可回滚。不要删除其他配置。
- 每轮上传使用新的服务端缓存目录，既有缓存不清空；首次转换必须核对真实调用与新增用量。重新转换使用应用本身的独立任务缓存。不把模型供应商的输入缓存命中误当成本地结果复用。

## 本轮验收范围

唯一样例名单取 `样例数据/样例登记.json`：01–05、S01–S05、X01–X04。每例覆盖 DeepSeek 和 Qwen 的新转换结果，所有操作通过公网浏览器入口完成，额外请求只用于核对浏览器所产生的任务和文件。故障模拟仅作用于测试浏览器，不破坏全站或真实用户任务。

逐例检查：

1. 首页、键盘入口、文件选择/拖入/替换/移除、多文件/格式/空文件/超限、两种模型、出版信息展开/收起及填写、开始转换。
2. 分片上传、失败重传、上传中导航保护、处理进度、复制链接、返回首页、最近记录再进入、刷新及断线恢复；转换失败和失效链接入口。
3. 结果页、全部顶栏及更多菜单入口、预览/XML 切换、目录收放、六种结构类型及搜索空结果；逐条点击该稿件实际目录，检查定位与内容显示。预览与原稿分段连续滚动，覆盖全文、图片、表格、公式、文献。
4. 原稿对照与下载、所有文章/出版字段和可用选项、往返保留草稿/滚动位置、保存/取消、各离开入口的确认与取消；只读面板的继续编辑；保存失败重试、版本冲突和异步保护。
5. 检查详情及实际提供的每个定位/修改/技术展开入口；用量的实际接口统计与导出一致。
6. 成果包、单独 XML、Word 原稿、文件说明与链接复制；文件名、ZIP 内容、XML 字节/版本、配套媒体一致；未保存提示、下载失败、旧版本提示。
7. 恢复自动结果（取消/确认），重新转换（模型切换、当前/首次出版设置、取消/确认、旧结果保留）；近期记录打开/移除/跨标签同步。
8. 桌面、笔记本、平板横竖屏、手机横竖屏中的实际结果和各面板；键盘焦点、Escape、Tab 往返、横向溢出、遮挡、阅读和按钮可用性。

无此内容的稿件检查对应空态并记为不适用，不假造图表或作者。可无限重复的操作不可能穷尽所有排列；通过逐例功能、状态分支和往返组合的明细矩阵验收，不把有限矩阵宣称为数学意义的全部路径。新增发现的路径补入矩阵，下轮全量重跑。

## 截图与收敛纪律

操作前后截图，操作过程中记录浏览器提供的连续渲染帧，保留时间戳、动作、任务和失败证据；长内容逐屏滚动覆盖，不只截首屏。浏览器连续帧接口不保证每个硬件刷新帧，须如实记录实际帧数和频率。所有采集的视觉状态实际打开审查；完全相同的图像可用哈希对应同一图，不能自动标记未看的图为通过。

每轮证据独立存 `reports/web-public-20260915/round-XX/`，包括操作日志、截图/连续帧、下载、任务/用量、矩阵及问题表。发现问题只记录并安全恢复测试现场，完成该轮后统一分析修复；阻断路径标明，不假报通过。验收脚本自身问题与产品问题分开登记，修复脚本后补跑被阻断的场景，不丢弃已有证据。

全量 Python/JavaScript 回归和覆盖率另行报告，不能将业务矩阵 100% 偷换为代码 100%。每轮须同时满足 14 例双模型新调用、逐例矩阵完成、所有截图审查完成、下载一致性及回归通过，且无未解决产品问题，才可报告收敛。API 额度或网络等外部阻碍如实说明，不复用旧结果替代全新转换。

## 当前进度

- 已重新核对上一轮完成记录、实际源代码、正式服务、14 例登记及原始需求。
- 原正式服务 PID 773812；14 个已有任务快照待逐份切换前核验。
- 原提交 ZIP SHA256：`ef4fc872c1f48295961fe264b498b0581b8808d58ee9839bfde732eafd2f3a41`。
- 已上线：固定发布树 `/home/denggf/word2jats-web-release-r01`，应用 `87d075b`，正式服务 PID 1304807；新增覆盖配置 `/etc/systemd/system/word2jats-web.service.d/zzzz-public-20260915.conf`。公网首页已实际截图检查；14 个原任务状态、XML 哈希、完整 llm 统计逐份核对一致。
- Git 计划提交 `175e92d`；标签 `web-public-before-20260915` 指向原正式版本，`web-public-candidate-20260915` 指向待验收应用。覆盖配置源文件为 `scripts/deployment/web-public-20260915.conf`。
- 第一轮进行中。`scripts.verify_web_public_convert` 从公网界面逐例双模型上传，`scripts.verify_web_public_actions` 对本轮新任务执行结果页操作。证据在 `reports/web-public-20260915/round-01`；不能把已有 `complete.json` 当成视觉验收完成，所有视觉审查尚待完成。
- 连续帧采用浏览器 screencast `everyNthFrame=1`，保留实际提供的每帧及时间戳；完全相同 JPEG 只保存一份但日志保留每帧关联。操作前后另有 PNG，正文逐屏滚动。采集期间证据会较大，应监测空间，不擅自删帧或抽样冒充全量。
- 首批 01 双模型已成功真实调用：DeepSeek 50 次/544092 Token，Qwen 51 次/578622 Token。02 已提交新调用。发生一次原稿核验请求超时，随后暴露测试记录器同名参数异常，导致测试脚本中断；只修复了测试记录器并提高文件下载核验超时，没有修改线上应用。02 接续原已发起的新任务，不再次上传复用缓存。中断前证据保留，续跑写入独立 continuation 目录。

### 12:09 接续记录（仍为第一轮，尚未统一修复）

- 原应用发布仍为 `87d075b`、PID 1304807。开发树只有验收脚本/记录更新，最新提交 `d1c2a14`；之后有少量测试脚本修改待提交，用户 AGENTS.md 仍不纳入。
- 正在运行的转换进程来自会话 90339（旧版 `verify_web_public_convert`，并发 2）；已推进至 S01，03 双模型实际转换完成，但 37 MB 原稿的辅助 API 下载超时，`convert/result.json` 尚未建立。重跑该脚本会通过现有 `task.json` 接续同一份新调用，禁止重传缓存代替。最新脚本已改为点击公网前端“下载 Word 原稿”后核对字节，与真实用户同一路径；不修改正式应用。
- 正在运行的结果页进程来自会话 98285（启动时只发现 01/02 四份成功记录）；01 已执行至响应式/恢复/重转。该进程加载的是补充前旧脚本，后续必须补跑 `remaining_entries`，并重新覆盖脚本时序问题阻断的路径。不能因 `complete.json` 存在就宣称该例全部通过；文件中的 issues、各步骤日志、视觉审查都要汇总。
- 基础回归：718 passed、5 skipped、1 个既有弃用警告，158.58 秒；本轮 `pytest.xml`、`coverage.json`。JavaScript 4 passed，session.js 行覆盖 97.62%、分支 97.78%。代码未变，线上后续功能问题修复后仍须全量重跑。
- 实际已查看 `visual-review/01-convert-screenshots/sheet-00001.jpg` 至 `00015.jpg`，98 张操作截图、59 个精确不同图，已登记。只有这批登记为已看；其他截图和所有连续帧仍未完成审查。初始 about:blank 是导航前证据，不算应用白屏；Qwen 首次结果出现编辑/目录暂未载入的退化状态，需结合请求时序继续核实，不能直接当作正常完整结果。

暂记问题（完成全轮后统一归因与修复）：

1. **产品恢复问题**：模拟原稿 503 后，关闭/重开对照继续复用同一个失败 iframe，不触发重新请求；下载 Word 可用。代码 `drawSource` 只在 frame 不存在或任务改变时设置 src，实际双模型重现。不得在本轮中途修复。
2. **测试时序**：原稿自动定位发生在测试滚动置顶之后，使 DeepSeek 首屏直接落在文献末尾，图片因 lazy loading 没加载；不是图片 404。截图 `01-deepseek/actions/continuation-1789444602660697594/screenshots/00274-S01 Word 原稿全文-0.png` 已原尺寸查看。新脚本等待 iframe load/字体完成再滚动并确认顶部；本轮须补跑完整滚动与正文定位，不能删除失败证据。
3. **测试时序**：普通两次 click 实际可能等第一次下载结束再发第二次，造成故障路由未结束即被撤销、Playwright `Route is already handled`。最新脚本改用真实 dblclick、等待故障响应发送完成并断言只发一次。需补跑受影响的下载/异常章节。原有错误记录保留。
4. **网络/核验路径**：辅助 API 原稿下载 30/180 秒超时（02/03），HTTP 已返回 200，浏览器与辅助请求出口不同。改用真实浏览器下载核对后再判定产品下载是否存在问题，不能仅扩大超时掩盖用户问题。

下一步保持首轮应用不变，完成余下 14 例双模型新转换与全操作、补足上述验收脚本阻断路径及新增遗漏入口；持续汇总但不提前修复产品。`scripts/review_web_public_images.py` 只生成/登记实际看过的图版，不自动判定通过。监测磁盘（12:06 本轮约 2.7 GB，初始可用 217 GB）；已有截图不删除。

### 12:30 接续记录（以此节为准，第一轮未完成）

- 首次转换进程 90339 已结束。28 份 `convert/result.json` 均已建立，覆盖全部 14 例 × DeepSeek / Qwen；均有真实调用、新增 Token，且本地响应复用为 0。03 双模型通过前端实际下载 37 MB Word 并核对原文件哈希后接续完成，没有重传或改用旧任务。
- 应用仍固定在发布树的 `87d075b`，不因测试发现问题而中途修改。开发树最近提交 `a0fdcc7` 之后只增加测试脚本的拖放/损坏 Word 补充路径、连续动作记录，以及续跑 trace 防覆盖；没有改应用。
- 会话 98285（PID 1315759）启动时仅包含 01 / 02 四组，使用旧脚本；01 两组和 02 Qwen 已执行其 7 章，02 DeepSeek 仍进行中。该旧脚本的 `complete.json` 只表示章已尝试，并非通过。四组均须补 `remaining_entries`、`upload_paths`，并补跑被测试时序问题及产品断点截断的路径。02 Qwen 重新转换前的真实保存收到公网 502，重转章未走完，需保留问题并验证重试退路。
- 会话 46490（PID 1332383）于 12:13:11 启动，只快照到当时已有结果：03、04、05、S01、S02、S03 双模型，以及 S04 DeepSeek（12:13:10 建立结果）。S04 Qwen 和 S05 / X01–X04 不在这批队列。该进程含强制刷新等时序修正，但不含 F05b 失败后的继续测试分支；因此原稿失败可能截断后续双页面冲突测试，需要补跑。
- 会话 76491 新启动，专测 S05、X01–X04 双模型，并发 2；使用最新脚本。与两个旧进程的稿件不重叠。不要另开进程修改这些同一任务。
- 新脚本 `reset_view` 已确保同一 URL 真正刷新；`exceptional_paths` 将失败原稿恢复单列为 F05b，记录断点后刷新继续双页面冲突，而不是直接结束整个章节。补跑用 `--repeat --chapters ...`；证据写到 continuation 子目录。`trace.zip` 也改为同一独立证据目录，避免补跑覆盖之前的 trace。
- 当前实际视觉审查登记：`01-convert-screenshots` 的 1–15 图版全部已看；`01-convert-frames` 仅 1–8（共 555 图版）；`01-deepseek-actions` 仅 1–12（共 136 图版）。其余均未看，不能当作通过。最后一批查看的是 01 DeepSeek 参考文献/搜索/摘要/文章信息的截图，未发现额外视觉问题。
- 03 Qwen 原稿侧栏下载出现两次 `Download.save_as: canceled`，需与其他入口下载成功事实分别核实；不能因首次转换下载成功就豁免侧栏路径。02 Qwen 编辑公网 502、原稿 iframe 恢复失败等均保留日志，完成全轮后统一归因修复。
- 12:26 本轮证据占用 8.1 GB，剩余约 206 GB。持续监测，不丢弃连续帧。结果页操作、被截断路径补跑和全部视觉审查远未完成，不能宣布收敛。

### 12:48 接续记录（第一轮仍未完成；不要提前修复应用）

- 重新读完最新用户要求、项目纪律和恢复记录。正式应用仍为 `87d075b`，服务 active、PID 1304807。没有修改提交材料或应用代码。开发树最新已提交 `9abb387`，新增的仅是连续帧审查工具及其 4 项自测，均通过。
- 全部 28 份首次转换已完成，初次转换进程 90339 已退出；原 01/02 操作进程 98285 也已退出。01、03、S05 双模型均已完成“当前/首次出版设置”的实际重新转换。02 双模型的重新转换仍须补跑，不能因章名出现在 complete.json 中而认定完成。
- 活跃操作队列：46490 / PID 1332383（03–S03 双模型、S04 DeepSeek，目前在 04）；76491 / PID 1346484（S05、X01–X04 双模型，目前 X01）；20265 / PID 1347565（01/02 补充，01 已完成、目前 02）；33723 / PID 1363865（S04 Qwen，单独补齐先前未纳入队列的一组）；78694 / PID 1365313（01 双模型 responsive、restore_and_recent 补跑）。启动新队列前核对进程和样例，不得同时操作同一个任务。
- 新增 `--providers` 筛选，供指定模型单独接续；不改变实际调用与验收范围。旧进程 46490 不含 upload_paths、F05b 后继续冲突测试的改进，必须在其完成后补上这些路径。当前启动的其余队列包含 upload_paths；01 双模型的真实损坏 Word、键盘选择、拖放、替换路径已执行。
- 多组平板/笔记本侧栏滚动中断：已实际查看 01 DeepSeek 的 00848–00851 截图，滚轮落在内部“所属单位”滚动区域，未作用于外侧栏。只修正验收脚本，将外侧栏全文滚动鼠标移到右侧留白，仍用真实滚轮，不直接设置滚动结果；78694 正在补验。这不是已修复的产品问题。旧进程加载的是修改前脚本，仍需补跑受阻响应式路径。
- 新发现并保留证据：03、01、S05 多处多篇文献引用链接使用整串 ID（如 `#b11%20b12%20b13`），点击找不到对应目标。原稿失败 iframe 重开不恢复仍可复现。S05 两模型原稿 o2 图片加载失败，部分 Word 下载取消；有实际 `net::ERR_NETWORK_CHANGED`，待全轮后结合请求记录区分网络与产品问题，不在中途定案或修复。
- 连续帧审查采用无损差分：每个源帧核对哈希，所有变化像素的矩形贴回前帧后须逐像素一致；每次动作改变保留整页上下文，完全相同图块共享审查。所有原帧保留，不按时间抽帧，不自动判通过。此方法已向用户说明。
- **实际视觉审查登记**：`visual-deltas/01-convert` 的 full 1–25、micro 1–63、strip 1–7 全部已打开审查并登记，覆盖 7626 个提供的连续帧事件、6470 个差异图块。`visual-review/01-convert-screenshots` 1–15 全部已看；`01-deepseek-actions` 仅 1–40 已看；`other-convert-screenshots` 本次已看 1–6，待写入登记。原 `01-convert-frames` 只实际看过 1–10，不能自动全选，该批余帧由无损差分关联审查，不虚报全图逐张打开。
- 图像生成会话 65561 正在处理 `visual-deltas/other-convert`，不代表已审查；此前 `other-convert-screenshots` 的 127 图版已全部生成，只有上述实际看过范围有效。多数结果页截图和连续帧尚未审查，本轮远未收敛。12:43 本轮占用约 15 GB，继续保留证据并监测空间。

### 13:00 接续记录（第一轮未完成）

- 应用和正式服务仍未变动。开发树最新 `c073c4a` 只修正恢复结果章节的独立测试前提：若任务已恢复原版，先核对原 XML 哈希，再通过界面实际修改并保存题名，然后验证恢复；不把正常禁用的恢复按钮当作故障。01 双模型 responsive 已完整通过；01 Qwen 恢复补验会话 65452 已结束、无问题记录。
- 01/02 补验会话 20265 已结束。02 Qwen 补跑的主要剩余问题为原稿失败后不恢复和多引用链接失效；02 DeepSeek 另有保存未成功、下载等待超时及一次 `ERR_NETWORK_CHANGED`，均保留证据，尚未统一归因。新会话 61265 / PID 1381421 正在补 02 双模型 responsive、reconvert；不得并发操作同一任务。
- S04 Qwen 单独会话 33723 已结束，九章均尝试；原稿 o2 图像加载失败、两次 Word 下载取消导致部分路径未走完。新会话 64563 只补此组 read_content、checks_and_downloads。此前 responsive、editing、restore、实际双次 reconvert、remaining_entries、upload_paths 均有通过记录。
- 原队列 46490 / PID 1332383 仍运行，已走完 04 DeepSeek 并进入后续样例，04 Qwen 重转仍在进行；76491 / PID 1346484 在 X01 收尾；44116 / PID 1370220 已补 03 双模型 read/checks/conflict/responsive/upload，正进入 S05。旧队列的缺失 upload_paths、冲突和响应式受阻路径仍需逐例补齐。不要因为 complete.json 有章名就认定覆盖完成。
- **首次转换全部操作截图已实际审查**：01 图版 1–15；其余 26 组 `other-convert-screenshots` 图版 1–127，1276 张截图、505 个精确不同图均已登记。01 首次转换连续帧审查保持上一节已完成状态。`01-deepseek-actions` 本次已打开 41–46，待登记；其余动作截图和连续帧仍未完成。
- 图像生成会话 65561 继续处理其余首次转换连续帧；新增 83207 处理 01 双模型所有结果页动作连续帧。两者都只生成无损差分审查材料，不自动登记审查或通过。13:00 本轮约 23 GB，全部证据保留。

### 13:28 接续记录（首轮尚未完成）

- 正式应用仍固定为 `87d075b`，服务正常运行；没有提前修复应用，也没有修改已提交材料。开发树 `89b5d7e` 增加验收进度核算和独立尺寸接续；验收工具的 7 项自测通过。AGENTS.md 的用户修改不纳入提交。
- 46490 / PID 1332383 队列推进到 05 的真实重新转换，后续为 S01–S03 双模型、S04 DeepSeek。76491 / PID 1346484 已完成 X02 并进入 X03 双模型。旧队列仍缺 upload_paths、原稿故障后继续版本冲突等补充路径，必须在该稿件退出旧队列后补验。
- 02 双模型的两次实际重转、响应式补验均已完成；02 DeepSeek 编辑与保存成果下载补验通过，既往失败记录保留。03 双模型的原稿/下载/异常/响应式/上传补验已结束。04 Qwen 的补验 22382、X01 双模型的补验 21042 已结束；69527 / PID 1407805 补验 04 DeepSeek 和 S05 DeepSeek 响应式，04 已通过，S05 仍有断点。不得并发修改同一任务。
- 新的明确视觉问题：04 DeepSeek 的 `actions/continuation-1789448921996149675/screenshots/00218-failure.png` 中，侧栏标题为“文章信息”，内容却是原稿 iframe，无法编辑；已打开原尺寸确认。补跑成功不能抹去该故障。另有 X01 预览 iframe 的 contentDocument 为 null、S05 不同面板异步失败等，先保留记录，整轮后统一归因。
- 新版 responsive 将六个尺寸独立执行；某一尺寸失败后保留证据并真实刷新，继续其他尺寸，避免一个产品断点使手机部分未执行。旧进程不会自动获得此改动，补验须核对其实际动作。
- 实际视觉审查：`01-deepseek-actions` 的 1–136 图版已全部打开并登记；范围仅限 `continuation-1789444602660697594`，不代表 01 的其他续跑。首次转换操作截图沿用前节已全部完成状态。`other-convert` 连续帧差分图版仅 full 1–12 已实际看过，micro / strip 尚未看；`01-convert` 三类差分仍为全部已看。
- 图像生成已完成：`visual-deltas/other-convert` 共 102238 帧记录、28228 个差分图块（micro 262 / strip 78 / full 211 图版）；`visual-deltas/01-actions` 共 43797 帧记录（micro 90 / strip 33 / full 1489 图版），后者均未审查。`visual-review/01-remaining-actions` 共 609 图版尚未看，只排除了与已审查图像哈希完全相同的副本，不抽帧，不自动批准。
- `scripts.audit_web_public_round` 汇总逐例实际新调用、章节/子步骤问题、真正审查过的图像哈希；早期没有 chapter-end 的原记录单列，不当成全章通过。新增可见控件与实际操作记录比对仅用于查漏；旧记录器未采集事件的控件须回查原步骤，不能直接报成产品问题。13:28 磁盘剩余约 166 GB，所有证据保留。

接续顺序：核对运行队列 → 补足各例实际未执行的路径 → 完成全部视觉审查 → 汇总整轮问题并统一修复 → 新发布树与新缓存上线 → 全量回归、14 例双模型全新调用和全部操作/视觉重跑。当前离全轮完成尚有大量工作，不能报告通过或收敛。
