# Web 决赛前定向修复与验收

## 目标与范围

2026-09-15 用户调整授权：停止等待第一轮全样例、全帧审查，以当前已发现问题为准立即修复并针对性验收，为现场决赛和 PPTX 准备留出时间。本计划替代《Web公网上线与14例验收-20260915.md》中“整轮全部完成后才修复、修复后完整重跑”的执行条件；旧证据保留，不将未审内容改判通过。

目标是可靠的 Word 转换、对照、关键字段修改与成果交付，不扩展功能，不重做视觉设计，不修改已提交的材料。Web 最迟北京时间 9 月 16 日 07:00 收敛上线。用户最新明确：PPTX 不属于当前轮任务，等待另行提示，禁止据总期限自行扩大到其他答辩准备。Web 必须为后续准备让出时间，不以用满期限为目标。

## 总体时间安排（北京时间）

- 9 月 15 日晚：集中修复、定向回归、正式发布；优先争取当晚结束，07:00 为最晚边界，不再恢复全量逐帧队列。
- 本轮 Web 核心修复完成、验证并上线后汇报结果，等待用户对 PPTX 的额外指示。本轮不创建 PPTX、讲稿、答问材料、邮件或其他材料，不改已提交方案及压缩包。
- 任何接续先确认这份总计划、最新用户要求和正在运行的任务。非关键美化或无法在窗口内安全解决的个别问题如实列出，不以重构完美为由延误比赛准备。

## 固定顺序

1. 保留 Git 回滚标签和固定发布树；仅恢复本轮测试留下的修改，不触碰真实用户稿件。
2. 按共同原因修复已确认问题：原稿/预览加载及面板切换、引用定位、编辑校验与自定义期刊、任务/下载提示、Word 兼容分支重复解析。
3. 用针对性单元/API 测试验证边界，以能触发同类问题的不同稿件执行浏览器检查；检查修改后 XML、成果包和用量不变性。运行现有完整自动回归。
4. 测试通过后发布新的固定工作树；公网验证实际修复、关键主流程和既有任务不变。失败则修正对应问题并重复相关验收，不重新堆积全量连续帧。
5. 明确交接已修、已验、剩余风险与回滚方法。恢复临时网卡设置及停止临时测试服务。
6. 用户要求收敛后清理 home 顶层的本任务临时文档与工作区：先只读盘点，确认任务归属和正在运行服务的依赖，再将必要内容归入项目、删除确认无用的临时内容。依赖工作区须先安全迁移并核验服务，禁止递归删除 home、其他项目、个人文件或会话记录。交接时列明移除范围及可恢复性。

## 验收口径

共同原因的修复不以一个样例通过代替所有情况：原稿加载覆盖失败重试及快速跨面板；引用覆盖单目标、多目标及不存在目标；编辑覆盖长表单、跨面板错误定位和输入保留；预览覆盖慢响应、失败及恢复版本；交付验证 XML 字节、版本和包内文件一致。转换解析仅消除明确的兼容分支重复，不用文本去重误删真实重复内容。

已有 28 组公网真实转换可以用于界面和交付回归；解析修复需另验受影响原稿及新产物。冻结产物夹具只证明界面/API 行为，不宣称重新运行了模型。截图仅记录本次关键状态并实际查看，报告不使用“全路径、全帧 100%”结论。

## 安全基线

- 正式站点：`https://word2jats.jianglab.work`，发布代码 `87d075b`，工作树 `/home/denggf/word2jats-web-release-r01`。
- 回滚标签：`web-public-before-targeted-fixes-20260915`；开发起点：`web-public-targeted-work-start-20260915`。
- 开发目录：`/home/denggf/word2jats-web-next`；用户 `AGENTS.md` 修改不提交、不覆盖。
- 已提交材料保持不变。临时网卡授权仅涉及 `enx1afdc44df54a`，不得改主网卡或隧道。

## 定向修复与发布进度

已修正原稿失败后无法重试、晚到响应覆盖编辑面板、多目标文献链接无法分别跳转、长表单校验错误难定位、自定义期刊显示空选项、下载失败残留旧成功提示、读取旧任务误显示排队、恢复后短暂显示旧预览等问题。预览失败可明确重试，并保留 XML 与成果下载入口；失败任务的重试说明不再混入成功结果口径。

Word 解析修正的是 `AlternateContent` 中同一文本框的两套兼容表示，不按文字去重。受影响的 01 保留两个实际文本框及原编号，避免旧任务原稿定位错位；其他 13 例的独立源对象核验通过。多文献预览沿用正文括号位置，不修改交付 XML。

发布前完整自动回归为 **824 项通过、5 项按既定条件跳过**，记录见 `pytest-final.xml`；47 项定向测试及 JavaScript 4 项测试也通过。隔离浏览器 `local-r4/result.json` 为 44 项通过、错误为零、4 个测试任务均恢复；原有用户流程 `existing-flows/验收清单.json` 为 11 组通过、61 张截图。上一轮 40 张定向截图已逐张检查，本轮追加检查变更状态与不同屏幕的关键截图；不将其表述为历史百万帧全部验收。

本轮证据统一位于开发工作树的 `reports/web-targeted-20260915/`。先前 S04、S05 的公网测试修改已经全部通过界面恢复，并核对 XML 摘要；不再继续旧全量队列。正式发布前检查没有活动任务，使用固定发布树与独立缓存；用户原有 14 个任务另有发布前摘要基线，发布后检查其结果与用量不变。

目录迁移安排：开发树迁至项目 `tmp/web-next`；旧发布树保留在 `tmp/web-release-r01`，新发布树位于 `tmp/web-release-r02`。发布与回滚配置分别为 `scripts/deployment/web-public-20260915.conf` 和 `web-public-20260915-rollback.conf`。只有依赖切换并验收后才视为清理完成。
