# Web 重构验收与交付记录

本文件为内部维护记录，不进入决赛提交包。批准范围与逐项定义见 [固定执行计划](Web重构执行计划-20260914.md)。

## 实现与验证结论

上传、转换、内容预览、原稿对照、文章与出版信息修改、结果下载构成同一条操作流程。人工保存更新实际 XML，另存版本，原自动结果可恢复。未增加整篇自由编辑、多人审核或转换核心改造。

| 验收项 | 结果与依据 |
|---|---|
| 全量回归 | 632 通过，5 条件跳过；reports/web-product-20260914/全量回归.txt |
| 功能矩阵 | 64/64；61 项由浏览器全流程覆盖，P11、E11、E18 由单元/API 与真实重启补充 |
| 独立视觉矩阵 | 29 组、58/58 张；已逐张查看桌面、窄屏、表单、定位、用量、正常与异常状态 |
| 浏览器错误 | 隔离全流程无未处理 JavaScript 错误；生产两配置成功流程无页面错误 |
| Web Python 语句覆盖 | 1126/1232，91.40% |
| Web Python 分支覆盖 | 403/474，85.02% |
| 编辑不改变其他内容 | 两配置各 14 份真实输出验证无操作字节一致、题名修改保留正文和文末；另测行内格式、作者关系、脚注与媒体 |
| 冻结转换核心 | 72 个批次源码哈希一致；其中 1 份非运行研究笔记不进入提交包，运行源码 71 份 |

100% 指批准需求和列明界面状态的完整覆盖，不指代码行覆盖或无穷输入组合。浏览器代码覆盖原始记录仅作辅助，不以一次页面生命周期的记录推算整个 JavaScript 应用覆盖率。

完整逐项证据位于 `reports/web-product-20260914/最终验收.json`；原始浏览器操作清单为 `验收清单.json`，后者的“待视觉检查”是脚本自动生成时的状态，最终人工视觉结果以 `最终验收.json` 为准。截图保留原始测试状态，不进入对外说明书。

5 项跳过为：1 项等价变换不适用、1 项无既有评估输出、3 项缺少原有集成测试专用模型缓存；不是重构新增功能的未测试项。生产实际转换另行验证。

## 线上切换

2026-09-14 19:11，确认活动任务为 0 后，保存旧内存服务中仍可查询的 8 个任务快照，重启 `word2jats-web`。8 个原任务均可打开，XML SHA-256 与切换前一致。另有 17 个历史目录在切换前已无法通过旧 API 查询，没有修改或删除这些目录。

真实浏览器通过公开域名上传独立 S03 稿件，并验证图片预览、原稿字节、字段保存、用量不变、下载中的当前 XML、恢复原始 XML 和刷新恢复：

| 配置 | 任务 | 结果 |
|---|---|---|
| DeepSeek | 4bc81f661b664ede | 全流程通过；32 次本地复用、1 次实际调用，输入 24,078、输出 1,303、缓存命中 23,680、未命中 398 |
| Qwen | 763e14aeed7c4d63 | 全流程通过；32 次本地复用，无新增模型用量 |

上述过程用于验证部署后的应用，不计入说明书实验。人工修改不新增模型调用，均已核对修改前后返回用量一致。首次 Qwen 验证中，服务器保存成功后浏览器未确认成功，输入仍保留；该独立测试任务已恢复原始 XML，随后重新上传的完整流程通过。原有用户任务未用于写入测试。

## 材料同步

说明书修改前备份为 `Web重构前说明书-20260914-sWR3hJ/` 下的 MD、DOCX、PDF。更新 Web 流程、受控编辑、结果版本和截图；总体架构图改为真实字段修订与当前文件导出。双模型质量、耗时、Token 与成本表不变。

说明书仍为 16 页，已逐页查看。正文黑色，中文宋体类 Noto Serif CJK、西文 Times New Roman，字体已嵌入 PDF；保留 18 个命名目的地和 33 处链接注释，内部链接指向实际 PDF 页对象。仅执行说明书生成，不生成 PPT。

## Git 与恢复

基线 `fb17bee`；回退标签 `pre-web-product-20260914`；工作分支 `finals-web-product-20260914`；计划提交 `7653337`；主要实现 `0a091ac`；验收及迁移 `222ae25`。用户既有 `AGENTS.md` 改动不提交、不还原。

回退时先检查活动任务，保留当前任务与修订目录，再在独立工作树使用回退标签启动旧服务。不要使用 `git reset --hard` 覆盖当前工作区。新任务持久化文件和人工修订独立保留，旧服务不具备新的编辑与任务恢复接口，回退前需评估旧链接使用情况。

提交包独立核验结果在完成后补记。
