# 重构交接盘点（2026-08-21）

> 本文固化 2026-08-21 对 `rebuild-core` 分支的一次全面只读盘点：重构做到了哪一步、这一轮成绩怎么读、代码与图纸差在哪、下一轮改进从哪些事实出发。全部结论来自 git 历史、运行产物与源码实证，出处随文标注。读本文前建议先读同目录《检查点B工作清单》第九、十节（运行事实与问题登记）与《施工图纸》附录 E（契约偏离登记），本文不重复其细节，只给全景。

## 一、仓库状态

- 提交全在一条线上：`da98383`（重构前基准，tag `pre-converter-rebuild-20260818`）→ master 段 9 个提交（阶段 0，2026-08-18 傍晚）→ rebuild-core 段 23 个提交（08-18 至 08-21，HEAD `60b59d5`）。master 停在 `cd065f3`，其全部历史被 rebuild-core 包含，**无分叉**；工作区干净。
- 远程 `origin/master` 停在 `38f9721`（重构前），39 个提交此前无异地备份（本次维护已推送，见文末）。
- 测试：`.venv/bin/python -m pytest` 全量 **367 通过 / 4 跳过 / 0 失败**。注意必须用项目虚拟环境；系统 Python 缺 fastapi、且其 libxml2 处理含中文的 DTD 绝对路径有缺陷，会产生大量假失败。
- 评测器与金标准的只读纪律核实无违反：`样例数据/` 零改动；`scripts/` 仅四个提交碰过——两个获准的编排层适配（`a48a6ea` 候选包清单、`206f022` 样例分组别名）、一个 L0 合法性判定收紧（`60b59d5` 根元素核对，附 14 例零差异证据）；评分与度量逻辑零改动。

## 二、重构完成度：图纸各阶段对照

| 图纸阶段 | 状态 | 依据 |
|---|---|---|
| 阶段 P（五个前置原型） | **跳过或以其他形式吸收**：P1 等价物是 `tests/test_gold_inventory.py`；P4 判真集、P5 分属性表格实验未执行 | 图纸附录 E 第 11 条 |
| 阶段 0（确定性安全修复） | 完成，检查点 A 通过 | 《检查点A差异归因》 |
| 阶段 1（源对象图、快照、无密钥重放） | 完成（`2fd7d59`） | `model/source.py`、`llm/replay.py`、`test_stage1_foundation.py` |
| 阶段 2（SemanticDoc v2 与渲染） | 完成（`6dba3aa`）：金标准反向装载、严格往返（ID 双射、媒体按字节）、来源映射 | `semantic/model.py`、`render/v2.py`、`tests/goldload.py` |
| 阶段 3（解析接入新契约） | 完成（`c72aeb7`）：脚注/尾注/文本框/域/修订、对象两类组、样式继承、源覆盖账 | `parse/docx_reader.py`、`parse/media.py`、`verify/ledger.py` |
| 阶段 4（理解层整体切换） | 完成（`4ddb948`→`0a851c4`）：落锚器、切条双实例＋裁决、全局归并、逐条析字段、引用实体匹配、11 道交付门 | `understand/ground.py`、`merge.py`、`pipeline.py:270-296` |
| 检查点 B | **三轮已跑、判失败**，进入逐项修复流程（B-01…B-23） | 《检查点B工作清单》 |
| 阶段 5 / 检查点 C | 未开始 | — |

检查点 B 失败后的修复期发生了一次架构反转：头部（文首元信息）从图纸的"摘抄＋落锚"改为**模型直接生成 JATS XML**（`bc39aa5` 起 15 个提交），摘要/关键词随后拆出退回指针制。此转向的完整脉络、配套开洞（`model` 来源、`model-head` 销账）与遗留缺陷见《检查点B工作清单》9.1 节与 B-20；它与图纸的契约裁决悬置中（图纸附录 E 第 1 条）。

## 三、当前成绩怎么读（integrated-057302a-r1，14 例全新一轮）

三组数字放在一起才是全貌（V1 缺陷条数，条数是问题清单规模不是质量总分）：

| 样例 | 基准 08-14 | rebuild 三轮范围 | 本轮 integrated |
|---|---:|---|---:|
| 01 | 79 | 138–157 | 155 |
| 02 | 24 | 267 | 186 |
| 03 | 26 | 163–166 | 73 |
| 04 | 8 | 223–227 | 90 |
| 05 | 61 | 113–557 | 104 |
| S01 | 6 | 72–82 | 31 |
| S02 | 4 | 120–188 | 76 |
| S03 | 43 | 206–229 | 113 |
| S04 | 8 | 70–72 | **1193** |
| S05 | 17 | 115–123 | 63 |
| X01 | 604 | 1078 | **120** |
| X02 | 250 | 244–259 | 263 |
| X03 | 103 | 125–160 | **65** |
| X04 | 1056 | 315–356 | **350** |

- **对比 rebuild 三轮**，头部重构与 B-01/B-02 修复带来广泛改善（03、04、S01、S02、S03、S05、X01、X03 大降）；S04 是唯一灾难性恶化，根因是装配契约漏洞（B-13），不是模型波动。
- **对比重构前基准**，10 个计分例全面倒退（276→2084）且 5 例出现 L0 error（基准全 0）。倒退主体集中三处：B-13 装配哑分支静默丢弃（S04 约 1162 条＋02 约 138 条）、B-14 图/表交叉引用整块缺席（77 条）、B-15/B-16/B-17/B-18 等各数十条。已逐一定位到代码行，见工作清单第十节。
- **X 组是重构价值的实证**：X01/X03/X04 参考文献精确恢复 30/35/70 条（基准时代为零，检查点 B 硬指标达标）；X03 作者—年份 bibr xref 命中 50 漏 1；X01 正文不再整段丢失（L1 518→60）。X02 基本未动，因 B-05/B-08（OLE 对象去向、公式 MathML）尚未处理。
- 14 例全部 `delivered=false`，倒在 `source_coverage`＋`understanding` 门（7 例另有 DTD 门）——**交付门按设计工作**：该拦的拦了、候选照常可评测。门本身有两处噪声待修（B-03 空白误报、B-23 切词假阳性）。

## 四、下一轮改进的事实起点

1. **唯一工作清单**是《检查点B工作清单》：旧条目 B-03/04/05/07/08/10 原样待办，新条目 B-13…B-23 已登记根因，处理顺序建议见其第十节末。纪律不变：一次一个问题、先讨论后动手、禁止样例专用补丁。
2. **两项悬置的架构裁决**优先级最高，多个条目被它们卡住：头部直出方向（B-16、B-20）；"逐字保源"与"渲染物/内嵌标记"的矛盾（B-15、B-21）。
3. **修复杠杆估算**（评测代理反事实实测＋估算）：修 B-13、B-14、B-16、B-17、B-18、B-19 与四类 DTD 违规约可挽回 1500+ 条，10 例余量约 500 上下——仍高于基准 276，因 S04 正文恢复后会带出自身的新差异；追平与反超要靠 B 组余项与阶段 5 专项。
4. **保留着的退路**（未裁决前不删）：指针制头部的整套装配代码（`assemble.py` 非 direct_head 分支约 467 行）、英文 `front_pass`（`passes.py:1181`）、旧 `build/xref.py::XrefResolver`（图/表 xref 能力参考件）。
5. 证据存档：检查点 B 期间散落 `/tmp` 的 52 项过程证据已归档 `reports/_evidence-archive-20260821/`（49MB，本机产物）。

## 五、本次维护动作备案（2026-08-21）

以当前实现为锚点的只读盘点与项目同步，未修改任何流程代码：工程记录自 `tmp/转换器改进/` 迁入本目录（tmp 原件保留未动）；《检查点B工作清单》补记第九、十节；《施工图纸》增附录 E；docs/ 各篇按代码现状同步；清理已脱离生产路径的旧管线死代码（清单与验证见迁移提交的说明）；master 与 rebuild-core 推送远程备份。
