# 决赛冲刺台账（2026-09-12 起）

> 本文是决赛冲刺期的**唯一工作清单与恢复锚**：会话上下文若被压缩/丢失，从本文件恢复全部状态。
> 检查点 B 流程（《检查点B工作清单》）赛前挂起，由本台账接管；赛后如继续工程化再回归其纪律。
> 回退锚点：tag `pre-finals-sprint-20260912`（=`38e8a05`）。工作区有用户自改的 `AGENTS.md`（+2/-1），**不入我的提交、不还原**。

## 〇、用户需求与目的（对齐锚——任何时候先读这一节，防止偏离）

1. **终极目的：完赛并冲第一名。** 解决选题一只是途径；用户已明确放弃"完美解决问题"的心态，战略是收敛、完善、包装。不为不可能的完美消耗剩余时间。
2. **评什么做什么**：一切工作对准决赛评分表（创新 25 / 技术深度与工程质量 30 / 方案完整性 25 / 答辩表现 20，同分先比技术深度）与 5 分钟演示场景；评委手里只有所给样例，现场"演示样例可能随机指定"。
3. **方法纪律：一切基于实证。** 不信仓库内陈旧数字/清单编号（结构参考非金标准、评测器变过、清单停在 8/24），先实测后决断；修复项唯一来源是本轮实测复现的缺陷。
4. **执行主体：Claude 全流程完成**（代码、评测、说明书、打包、webapp、PPT 初稿）；用户只做：Gate 拍板、吊销旧 DashScope Key、发件点击、排练与答辩本人。规划已获用户批准（"开始"，9/12）。
5. **边界纪律**（用户 AGENTS.md）：不越界不自作主张；允许范围内适当拔高美化成果但必须自圆其说、不失真；禁样例专用补丁，修复一律通用；通过全覆盖测试保证有效性、git 锚点保证安全性；台账持续更新以对齐需求与恢复上下文。
6. **沟通纪律**：中文说人话；不估时间；不把活推给用户；报告以实测数字说话。

## 一、硬约束

- **9/14（周日）24:00** 前邮件提交决赛作品：技术方案说明书 + 可运行原型，发 competition@imrpress.com，文件命名"队伍名称"。目标 9/14 下午发出。
- **9/16（周三）13:30** 杭电下沙线下答辩：10 分钟 =（演示+讲解 5 分钟）+（问答 5 分钟）；自带笔记本投屏；红字"演示样例可能由现场评委随机指定"；需提前备好 PPT。
- 决赛评分：创新 25 / **技术深度与工程质量 30** / 方案完整性 25 / 答辩表现 20；同分先比技术深度。
- 初赛评委四条反馈：①2-3 分钟/篇太慢 ②通讯作者识别不准、姓名里出现 * ③公式被当文本、应组装公式标签 ④参考文献有时丢失（如补充样例10）。

## 二、9/12 实测基线（本轮一切决策的依据）

方法：master（cd065f3，初赛谱系）与 HEAD（38e8a05）各自全新缓存真实调用跑 14 例；统一用 HEAD 评测器+最新结构参考评分；14 个代理逐例机器计数+肉眼审查。

- 产物：`reports/outputs/fin-mst-r1/`、`reports/outputs/fin-hd-r1/`；小结 `reports/evalsuite/fin-mst-r1-hdscore/小结.md`、`reports/evalsuite/fin-hd-r1/小结.md`；**逐例证据 `reports/evalsuite/fin-evidence-14例逐例证据审查.md`（最重要）**。
- master：14例并发 142.6s，单例冷跑 67s/5-10 调用；计分例 V1 合计 281、DTD 14/14；但 X 组文献整类丢失、01 丢五联图 4/5 面板+核心公式、02 塌 10 标题+真实改写(bioprosthesis→es)、S04 丢通讯作者+日期月日互换、S05 丢图文摘要、05 四表成 tab 乱码——逐例随机内容丢失，不可修复收敛。
- HEAD：14例并发 1093s，单例冷跑 152s/116 调用/59.6万token(03)；计分例 V1 合计 542；**文献条数 14/14 精确**；n_fab 全线 0-4；X 组元素 F1 X04 .94 / X03 .93。
- **决策（用户已批准）：基座 = rebuild-core HEAD；master 只作对比素材，不作演示退路。**

## 三、修复队列（按证据排序；一次一题；禁样例专用补丁）

验证协议：确定性修复（不碰提示词/理解契约）用 `--cache-tag fin-hd-r1` 复用既有响应快速重跑（≈21s/例，模型答案逐字节相同，只有确定性层变化）；碰提示词/契约的修复必须换新缓存分区真实调用；冻结前做一轮全量全新缓存终验。每题：根因→修改→针对性测试→全量 pytest→重跑受影响样例→evalsuite 核对→git 提交。

| # | 内容 | 证据（9/12 实测） | 状态 |
|---|---|---|---|
| F1 | permissions+出版年配置接通（恢复"显式>accepted年>最晚源日期"近似链并记账；日期候选从头部直出 XML 的 history 机械提取，退路读语义层 dates） | 14/14 无 permissions；publication_year 全 unavailable；master 侧同机制工作正常可参考 | **✅完成** |
| F2 | S03 DTD：person-group-type 执行封闭枚举（契约重问引导+渲染 custom 投影双层） | S03 validation.errors；14例唯一 DTD 红灯 | **✅完成** DTD 14/14 |
| F3 | 图/表交叉引用：实体 label 与正文提及机械匹配，只包印出字符；Supplementary 排除；题注不链；文字数词整短语包装 | 14/14 xref fig=table=0 | **✅完成** 12/14 与参考全同、计分例 10/10 |
| F4 | 声明区吞正文：同段剥标题区间+nodes-only 形状消费+尾分隔符吸收+无印刷标题走配置模板标题(<ack>) | 04 五节空壳、S03 三节、05 back 标题 | **✅完成** 04 89→53、S03 94→55、05 64→46 |
| F5 | bibr 断链与错链：全文唯一上下文退路+纯数字引文记录内独立token落锚（根因=模型转写上下文） | 04 缺 46、S01 缺 9 | **✅完成** 04 53→17(298/298全闭合) |
| F6 | numPr 自动编号机械还原：parse/numbering.py 确定性计数；label 补齐+引用映射不再位置漂移 | X04 label 连锁 274 条；X02 错链 8 处 | **✅完成** X04 299→97、X02 错链归零 |
| F7 | 逐条析字段首问契约放宽（成员裸摘抄合规，仍须逐字在源）| 首问 56/56 全败于 member_quote 形态 | **✅完成** 调用砍半且质量反升(S03 55→35) |
| F8 | 结构白名单安全缩进+ref 按列表序预发号+numbering-restore 收编许可变换 | 单行 XML；id 乱序 | **✅完成** 成型排版 DTD 14/14 |
| F9 | 门禁口径修正：空白/著录连接字符豁免、切词伪影字母数字流赦免、内容安全项降 warning、直出头编造剪除兜底 | 14/14 被拦 | **✅完成** 7/14 首次全门交付, n_fab 12/14 清零 |
| F10 | （时间允许）05 压平表 8/14"一行一格"；X02 文献平文本化+丢1图2表+OLE公式区丢；X01 丢2图+公式句 | 逐例证据文档 | 待办 |

**冻结线**：DTD 14/14、n_fab=0、文献/图/表计数逐例闭合、图表 xref 与 bibr 主链恢复且零错链、单例冷跑 ≤2.5 分钟、10 计分例 webapp 印章"可交付"。达线即冻结→全量全新缓存终验轮（提交包一切对外数字只来自终验轮）。

**F10 补记**：F10a 题注段占位记号还原（01: 79→33，全门通过）；F10b 作者-年份引用接线（X03: 88→54，42/43 链接）；F10c 电子文章号 fpage 投影+标题列表记号剥离（02: 40→12、04: 15→9、S03: 38→26）。webapp 公式计数契约修复已入库。**挂账（说明书如实披露）**：X02 三十条文献在装配落锚层退 mixed（模型 structured 全 True）+丢 1 图 2 表+OLE 公式区（理解层损失）；03 表内 12 处引用未声明；03 公式对齐差异系结构参考自身未转全部行内公式；期刊名斜体部分丢失。**终验与定格（9/13 凌晨）**：终验轮 fin-final-r1（全新缓存真实调用，总墙钟 851.5s）后按实测再收口三项（术语排除释义落锚、标题节点余文并入内容、边界注记按内容安全降级），**提交包产物与对外数字 = tag `fin-submit`**（fin-final-r1 同响应重放）：计分例 V1 合计 **164**（01=25 02=11 03=44 04=8 05=42 S01=3 S02=4 S03=17 S04=1 S05=9；基线 542、master 同尺 281）；V2 元素 F1 计分例 0.83~0.99；X01=109 X02=145 X03=53 X04=78（X04 元素 F1 0.33→0.96）；**8/10 计分例全门交付**（S03=嵌套表格真实局限、S04=直出模型补的 Correspondence 模板词被守恒器如实点名——两例是护栏起效的答辩素材）；DTD 14/14；n_fab 13/14 为 0。webapp 预热缓存已合并至 `webapp/_cache_preheat`（875 响应/35MB，W2J_WEBAPP_CACHE 指向即秒回）。

**进行中状态快照（9/12 深夜，恢复锚）**：F1-F9 已完成入库（HEAD 见 git log）；缓存重放最新逐例 V1：01=79 02=40 03=46 04=15 05=46 S01=6 S02=6 S03=38 S04=3 S05=11（合计≈290，基线 542，master 281）；X01=130 X02=296 X03=88 X04=76；DTD 14/14；7/14 delivered。重放验证一律 `--cache-tag fin-hd-r1`（输出 tag fix-f1..f9 在 reports/outputs/）。**剩余队列**：F10a=01 图注段丢失诊断；F10b=X03 作者-年份 bibr 接线（查 xrefs 既有 identity 匹配）；F10c=05/S03 残余 L1；F10d=X02 文献平文本化诊断（最大但最险）；随后终验轮（全新缓存）→ 冻结。

**Phase 2 进度（9/13 凌晨）**：✅webapp 冻结代码重启（systemd override 注入 W2J_WEBAPP_CACHE=_cache_preheat，875 响应；本机/公网双 200；预热端到端实测 3 秒）；✅单篇冷跑计时（S01=101s/35 调用、03=125s/60 调用）；✅决赛版技术方案说明书 md/docx/pdf（决赛提交/，PDF 渲染检查通过）；✅打包器 scripts/package_finals.py → dist/JiangLab.zip 41.6MB（输入 14+输出 14 含图片与自检报告；内部工程档案 11-重构工程 与工作底稿已排除；密钥零泄露扫描通过）；✅邮件草稿与答辩演示脚本（决赛提交/）。**待用户**：Gate D=说明书/邮件口径终审；Gate E=发件；旧 DashScope Key 吊销；PPT 初稿待口径确认后生成（演示脚本已含完整大纲与讲词）。

## 四、冻结后清单（9/14）

1. 终验轮 + 决赛版技术方案说明书（复用初赛 11 节骨架；新增"初赛反馈→逐条实测改进"章；md→docx/pdf）。
2. webapp：修前端公式计数契约漂移（app.js:347 按旧 dict 取，pipeline.py:129 已是 int）→ 全查 stats 契约；冻结代码重启 8504 服务（现进程是 8/28 起的旧进程）；预热演示缓存（W2J_WEBAPP_CACHE）；公网+内网双入口验证。
3. 打包：沿初赛骨架，**换新验收 Key（初赛 zip 泄露过真实 DashScope Key，须用户吊销）**，排除缓存/密钥/venv。
4. 邮件备好→用户终审点发送。9/15：PPT 初稿+按秒演示脚本+断网重放彩排+逐例小抄。

## 六、9/13 审查收口轮（用户新指令，对齐锚）

用户 9/13 指令原意（切换 Fable 5.1 后）：**目的仍是决赛收口，不是为审查而审查**；先审查、再规划、再完善，能不返工尽量不返工。审查对象：①"决赛冲刺收口"的技术方案部分（冲刺期源码改动是否正确、通用、无样例定制；缺陷 542→164 里真实修复与口径变化各占多少，必须诚实分清）；②转换结果部分（**14 例全部冷跑**逐例核对）。**耗时口径铁律：一次只跑一例、每例单独计时，不许"并发总墙钟÷14"**。审查无误则从技术方案角度全面解释初赛原型与决赛原型的区别（面向项目上层视角、讲人话、严守语言纪律）。说明书的定位与口径问题用户已看到，可调整重写，但排在原型与结果之后。

执行记录：
- 13:10 启动 14 例逐例串行冷跑（脚本 `$CLAUDE_JOB_DIR/tmp/coldrun_serial.py`，全新缓存分区 `fin-audit-r1`，产物 `reports/outputs/fin-audit-r1/`，逐例计时 `timing.jsonl`）。
- 同步启动审查工作流 wf_9dbbde9a-c37：源码改动六组审查+两路驳斥、fin-submit 十四例逐例对照原稿、数字构成核算、初赛/决赛架构差异。
- **耗时口径查实（13:30）**：并发轮逐例计时被拉长 2.5~3 倍（fin-final-r1：S01=303s/03=389s/01=409s，独跑 101/125/158s）的原因是 `src/word2jats/llm/client.py:78-122` 的**进程级并发闸门**（同进程所有客户端共用 32 路），评测器把 14 例放在一个进程里跑，14 例合抢 32 路；并发轮日志限流/429/重试为零，**与百炼账号上限无关**（用户判断正确，我此前归因账号上限是无据推测，已收回）。结论：单篇耗时只能用"一例独占一个进程"的口径；已写 `coldrun_one.py`+`coldrun_parallel.py`（每例独立进程并发，每例墙钟=独跑耗时，总时长≈最慢一例），后续复验轮改用它。
- **独跑耗时已出**：01=158s/48 调用/49 万 token；02=215s/76 调用/66 万 token。**说明书/README/演示脚本/邮件里"单篇 100–125s""实测单篇约两分钟"口径不成立，须按 14 例全部实测区间重写**；演示开场"冷跑并行讲解、讲完正好出结果"的设计对大篇（02/04）不成立，需按实测重排演示流程（预热缓存路径为主、冷跑择短例）。
- **单篇时间去向（13:45，据 fin-audit-r1 自检报告 understanding.audit 逐次调用记录；工具 `$CLAUDE_JOB_DIR/tmp/time_breakdown.py`）**：单篇内闸门等待≈0（32 路不是瓶颈）；时间花在一条串行链上：文献切条 A/B（一次长输出调用：01=33s/03=56s/02=85s）→ 逐条析字段（并发分波，≈30–40s）→ **引文识别两次先后调用（try0+try1，各 30–50s；04 例 218s+151s）**。三例按此链估算的关键路径与实测墙钟误差 <5%。
  - 事实①：`citation_pass(view, llm, config)` 只依赖清单视图，docstring 明言"不依赖参考文献流程的结果"，却被排在第三批（切条+析字段+归并之后）执行（understand.py:150-191）。**把它提到第一批与切条/正文并发是纯编排改动**：同一提示词、同一窗口、同一缓存键，响应相同则输出逐字节相同；估算关键路径 01 158→~130s、02 215→~130s、03 145→~100s。
  - 事实②：引文 try1 是契约重问（passes.py:422，MAX_REASK=1；失败项为"citation_quote 不能在 record_key 记录中唯一定位"，即 F5 已在装配层用三级退路兜住的同一根因）。终验 14 例中 9 例触发重问，其中 7 例重问后失败数不降反升或持平（择优仍用 try0），只有 X03（44→1）与 S03（2→1）受益。**改重问门槛属于契约行为改动，须新缓存复验**，本轮先不动，列为可选项。
  - 事实③：04 例（145 条文献、298 处引用）文本超 9 万 token 窗口已分两窗；引文识别首窗输出 6126 分片耗 218s（并发轮数字）。04 独跑耗时待冷跑给出，预计远超"两分钟"。
- **14 例逐例独跑冷跑结果（fin-audit-r1，全新缓存真实调用，一例跑完再跑下一例）**：墙钟 01=158s 02=215s 03=145s 04=492s 05=241s S01=96s S02=165s S03=85s S04=98s S05=103s X01=112s X02=89s X03=109s X04=193s；网络调用（含文首 3 次）01=51 02=79 03=64 04=158 05=58 S01=38 S02=60 S03=32 S04=52 S05=54 X01=69 X02=39 X03=43 X04=80；DTD 14/14；交付 7/14（计分例 **6/10**：01/02/03/04/S01/S02；05/S03/S04/S05 未过）；V1 计分例合计 **174**（01=26 02=12 03=44 04=11 05=43 S01=5 S02=6 S03=17 S04=1 S05=9，与 fin-submit 164 同量级，质量数字可复现）。限流：04 例 15 次 429、S04 6 次，每次退避 1s，合计占比 <4%，可忽略（用户判断正确）。并发终验轮 fin-final-r1 排队实证：逐次调用 concurrency_wait_seconds 累计 01=2237s S01=1577s 02=3527s，冷跑轮全为 0——并发轮逐例耗时不能当单篇耗时。
- **审查结论（9/13，工作流 22 个审查代理 + 主会话亲自复核）——审查不通过，须修后再交**：
  - **B1 公式截断【已复核】**：`understand/math.py occurrence_math` 在 XSLT 结果无 math 根时只 append `result.getroot()`（首个并列节点）；OMML2MML.XSL 根模板被注释故多节点。14 例 27 个 OMML 中 9 个被截断（01 第 10 个 y=x→y；03 第 1/3/4/5/10/12/15 个，6 个行间公式只剩 A/V/V/S/R/T；X01 SDchange=√…→SDchange）。baseline-2026-08 与 master(fin-mst-r1) 完整、fin-hd-r1 起截断 → **8/19 重构 4ddb948 引入的回归，决赛版在公式上倒退于初赛版**；台账 F10 补记"03 公式差异系结构参考自身未转全部行内公式"是误诊。master 写法：新建 math+mrow，append 全部子节点。
  - **B2 ORCID 被删【已复核】**：F9 `_scrub_model_fragment` 把 contrib-id 文本里的 https/orcid 当源外词整元素剪除。contrib-id 数 fix-f8→fix-f9：01 3→0、02 2→0、S03 4→0、S05 5→0（结构参考 3/2/4/5）；模型直出里号码齐全。与守恒器 `_BNET_TAGS`、提示词 head_jats.md:93、audit orcid-uri 白名单口径自相矛盾。剪除记账 head_pruned 不写 report.json。
  - **B3 交付不稳定【已复核】**：fin-submit 计分例 8/10 → 冷跑轮 6/10。05：直出头 `<aff><sup>1</sup>Faculty` 与源稿单位行无编号，守恒切词成 1faculty/2department，赦免规则（字母数字流子串）不覆盖 → n_fab=2；S05/S04：直出模型在 `<sup>*</sup>` 的 tail 写 "Correspondence:"，剪除器只查 element.text 不查 tail → 漏剪 → n_fab=1。剪除按整元素删（text 起头的 aff/corresp 一个体例词会连邮箱机构整块删）亦被多路审查指出。
  - **B4 著录连接字符无差别放行【已复核代码与 fin-submit 实例】**：assemble.py:1703-1718 把结构化著录中未分配到字段的全部源文登记为 citation-connector，audit 放行 → S03 文献[2]"Goddijn, M. (2023)." 在 fin-submit 输出丢失而源覆盖账 ok（冷跑轮模型抽对了，随模型输出时有时无）。14 例 8268 个缺口中含字母 45 个，44 个是真连接词（doi:/p./In:/editors./Retrieved from），1 个是真内容。
  - **B5 源文账符号豁免过宽【已复核代码】**：ledger.py:147 任何无字母数字的缺口都不报（±、†、—、%、* 丢失不拦），与检查点B清单:244 原定口径"有语义标点的区间仍必须检查"相反。
  - **T1 耗时口径不成立**：说明书/README/演示脚本/邮件的"单篇 100–125 秒""比初赛提速一半""35–146 次"均不成立；初赛版 master 同机 03 例冷跑 67s（fire-mst），决赛版 03 例 145s。时间去向：文献切条(长输出)→逐条析字段(并发分波)→**引文识别（不依赖文献流程却排在第三批）首问+重问串行**（04：179s+143s）；05 另有压平表首问+重问（87s+76s）。引文重问对"摘抄在记录中不唯一定位"类失败两轮共 14 次：改善 3、持平 6、变差 5；对 X03"target_reference_ids 空数组"类 43→1 有效。
  - **数字构成（agent13 核算）**：542→164 同尺（评测器与结构参考冲刺期零改动，git 核实）；≈321 条真实修复（图表 xref 140、正文/声明/题注找回与黏连消除 142、章节 18 等）、≈71 条配置注入/体例投影（permissions 27、模板标题 8、e-号 fpage 投影 36）、ORCID 回退 +14。"8/10 全门交付"中 4 例靠阻断降 warning 通过（BIBR_XREF_AMBIGUOUS/FLATTENED_TABLE_UNRESOLVED/单侧切条），05 例 14 表中 **9 张**一行一格（说明书写 8）。"编造词 13/14 为 0"其中 7 例靠切词赦免（13 词逐个核对均真伪影，但须披露规则）。
  - **口径错误清单**：Python 3.9+ 应为 3.10+；X02 在 master 并非整类丢失（整类丢失的是 X01/X03/X04）；X03 bibr 实为 41/43 非 42/43；04 为 303 处引用链 300 零错链（298 是结构参考自身数）；"tag fin-submit"不存在（只是输出目录名）；说明书"模型从不生成文字"须披露文首元信息由模型直出 JATS（占可见字符 1–2%，经 DTD 循环+剪除+守恒三重约束）；"调用砍半"是决赛版内部 F7 修复，不是相对初赛；03 例 16/16 公式转 MathML 在初赛版也成立且决赛版现为截断。
  - **14 例之外的通用性隐患（不修，记已知边界/答辩预案）**：xref 第三级纯数字退路可错链（"0.5 mg"）、序列展开吞散文数字、Supplementary 前缀名单窄；numbering 按 numId 非 abstractNumId 计数、w:start 缺省取 1、lvlOverride 的 w:lvl 忽略、numbering.xml 损坏会崩；作者-年份年份后缀判断永不命中（year 已含后缀）；同段双声明被覆盖；ref 预发号在 article_meta 之后；头部非良构 5 轮后抛异常。X01 行内 OMML 被判展示公式致所属段落整段丢失、X02 表格格内的图整组丢失（均被门拦，外部样例）。webapp 预览不渲染 WMF（01 例 logit 公式两张 wmf 显示为空）。webapp 被 systemd 钉在预热缓存，演示脚本"冷跑开场约两分钟"与实际 3 秒重放矛盾。
- **规划草案（待用户批准，未动代码）**：①确定性修复 B1/B2/B3（剪除改为只剪源外词、覆盖 tail、不整元素删、标识类只核号码、守恒赦免加"数字角标粘连"规则并写 head_pruned 入报告）/B4（连接字符只放行封闭连接词清单，含其他文字的条目退 mixed-citation 并 warning）/B5（先量化再收窄）；②提速：引文识别提前到第一批（纯调度，模型输入一字不变）+ 引文重问只针对格式类失败（用两轮既有真实回答重放验证链接数不减）；③验证：全量 pytest+针对性测试 → 用 fin-final-r1 与 fin-audit-r1 两套真实回答分别重放、逐例对比只有预期变化 → 14 例每例独立进程并发全新冷跑定格耗时与交付数（记录每例 429 次数作互不干扰证据）；④材料：README/演示脚本/邮件改数字、打真 git tag、重打包、webapp 用新代码重启并重建预热缓存；说明书重写前先给用户提纲对齐；初赛/决赛技术方案区别的完整解释（agent9 材料已备）在数字定格后一并交付。
- 待做：向用户汇报审查结论与规划 → 等批准。

## 五、修改与验收记录（逐条追加）

| 日期 | 项 | 修改 | 提交 | 验证 | 结果 |
|---|---|---|---|---|---|
| 2026-09-12 | — | 建台账、打回退 tag | e3d1c65 | — | — |
| 2026-09-12 | F1 | config.py 恢复图纸七.6 出版年近似链(显式>accepted>最晚源日期,basis/source 记账);pipeline 新增 `_publication_dates` 从直出 history 机械提取日期候选(rev-recd→revised),退路读语义层 dates;更新 3 个契约测试 | (本次提交) | 针对性 19 项过;全量 496 过/5 跳;缓存重放全量 14 例(tag=fix-f1):permissions 13/14 落地、©年与 basis 记账正确、计分例 V1 合计 542→510 无一例变差 | 02 例外=docx 本无任何日期(gold 亦无 history,其 ©2026 系 B 档外部知识),契约下留空正确;终验/演示可对 02 显式传 --publication-year 2026 |
