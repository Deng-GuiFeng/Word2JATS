# 决赛提交前收尾计划与验收

## Compact 后必须重读

以 2026-09-14 用户最新一轮指令为准，距离提交约 4 小时。当前优先交付不可延期的技术方案说明书和可运行原型；Web 只处理明确提出的用量与下载命名，不展开新功能重构。此前 Web 重构已经完成，不重复执行旧计划。先读 AGENTS.md、本文件、最新用户消息；必要时回读原 Claude 会话第一条长指令。

说明书面向了解领域和比赛、但不了解本项目的评委。站在参赛团队立场清楚展示已有方法、结果与使用价值；不写内部执行记录、不自说自话、不为了包装编造事实。保持六部分实质内容、完整评价口径及冻结实测结果，不重新做核心算法与实验。只修正这轮明确暴露的内容、术语、版式与交付边界问题。

## 固定执行顺序与完成标准

1. **安全基线**：Git 回退标签 `pre-finals-closeout-20260914`；旧说明书及生成/打包脚本备份于 `提交前备份-20260914-fcioIi/`。不提交或还原用户 AGENTS.md。旧 dist 由打包程序完整归档。
2. **用量与成果下载**：从真实调用响应及其持久缓存读取结果用量；另列本次新增，不因本地复用显示假零，不从文字估算。兼容旧任务和缺字段缓存，确实缺失则标明缺失，不能凭样例匹配借用其他运行的用量。转换成果采用明确统一的包名和 XML 名，媒体路径与引用完整，附属记录不抢主次。
3. **说明书**：项目名称 Word2JATS；用户层面统一 Word 稿件（首次说明 .docx）；技术代码标识保留原大小写。删除含混的“工作台”等称谓，章节层级与术语一致。标准单列两级目录，真实链接；正文两端对齐、黑色、中文宋体类和西文 Times New Roman；表格严格不超正文边界，短字段适度居中；自然分页，避免硬分页及连排引发大片留白；重新截取高分辨率真实界面。Word 与 PDF 同步、逐页实际查看。
4. **原型边界**：JiangLab 提交包区分技术说明 PDF/Word 与可运行原型。原型仅转换源码、Web、运行资源、依赖与配置模板、许可和独立可用的运行说明。样例原稿、结构参考、模型缓存、历史输出、评测/制稿/内部记录不进入原型，不另塞回提交包。程序实际读取的提示词（含既有示例）、DTD、期刊配置及样式表是运行资源，不误删。
5. **最终验收**：相关新增单元/API 用例、全量回归；受影响 Web 状态独立截图；Word/PDF 全页版式与层级/表宽/链接检查；提交包独立解压、文件清单、依赖和 CLI/Web 实际转换、XML/媒体/用量/下载一致性验证。生产切换前检查活动任务，保留旧任务。测试记录在内部报告，不放入说明书。

## 状态

- 计划已固定，回退点与备份已建立。
- 用量与下载、说明书排版与截图、原型精简已经完成。说明书 15 页，逐页查看通过，单列两级目录带实际页码和链接；旧说明书和旧提交包均保留。
- 全量回归 645 项通过、5 项既有条件跳过；本轮新增计量、命名与排版测试通过。独立浏览器 61 项功能完成，剩余 3 项由已有单元/API 与实际重启覆盖；原 58 张状态截图逐张复查，用量 4 张以最终重截替代，另增旧记录缺失与窄屏复用 2 张。
- 独立解压原型，在全新 Python 环境安装；S03 两种模型真实调用、同稿复用、修改、下载和恢复全部通过；CLI 真实 DeepSeek 转换通过。复用前后 XML 与原始计量一致，本次新增为零。逐调用缓存原始 usage、下载计量、XML 引用和媒体原字节均已核实。
- 线上切换前确认活动任务为 0；11 份旧任务通过禁用网络的缓存回放、缓存时间、调用数与原始 XML 逐字比对，补回缺失原始计量。12 份已有任务切换后 XML 和原新增量不变、计量完整；原快照备份在 reports/finals-closeout-20260914/历史用量恢复/。
- 待办：最后的展示术语调整打入提交包 → 最终包独立解压复核 → 汇总验收与交付。禁止再扩展功能或改动冻结实验。
- 本轮允许为用量追溯添加必要的计量记录，不改变模型请求、缓存键、识别提示、转换语义或冻结实验。若源文件哈希变化，清单准确记录，不沿用“转换源码逐字不变”的旧结论。
- 技能：project-docs 管理文档与内部证据；agent-browser、agent-browser-verify、verification 用于实际 UI→接口→文件验证。无子代理授权，不使用子代理。
