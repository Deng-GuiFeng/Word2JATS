# 决赛提交前最终验收

本文记录上一轮 Word/PDF 与 ZIP 定稿。用户随后调整为仅交付可检查的 PDF＋原型目录，并要求预置测试配置及 A800 独立验收；当前交付状态以[最终目录交付与异机验收](最终目录交付-20260914.md)为准，不再生成 ZIP。

本文件用于项目维护和交接，不进入提交包。执行范围见[固定收尾计划](提交前收尾-20260914.md)。本轮只完成技术方案说明书、可运行原型及用量与成果下载的必要修正，没有改动识别提示、模型请求或冻结实验，没有制作 PPT 或邮件。

## 最终交付

提交文件为 `dist/JiangLab.zip`，约 2.76 MiB。包内包含技术方案说明书 Word/PDF，以及单独的 `可运行原型/`。清单记录 168 个文件的 SHA-256，另含清单文件本身。包内没有样例数据、结构参考、历史输出、模型缓存、密钥、评测和制稿脚本、测试或内部文档。

源码版本：`c22ff3c5b4ff957d09d5effbc2ca8252d4e83fc2`。提交包 SHA-256：

`bb86d262d6e2661c49aafa72307cb054037da495879f795a9596dfd485d3faba`

| 材料 | SHA-256 |
|---|---|
| 技术方案说明书.pdf | 7474f3ad0b56899f22338549430f60eeee50634daf5eafffeb69a63dabdd684f |
| 技术方案说明书.docx | 1b81af560314f7a96c05781b4528be24cf4857f3b903a2e3fb300e2382c49363 |

包内 Word/PDF 与 `决赛提交/` 下的文件逐字节一致。原型 README 来自[原型运行说明](../原型运行说明.md)，包含环境、安装、密钥配置、网页与命令行使用、成果解压和必要故障处理。提示词、DTD、公式转换样式表及预览组件属于运行资源，随原型保留。

## 说明书

面向了解比赛与问题领域、但不了解项目的评委组织内容；统一产品名称 Word2JATS，用户层面使用 Word 稿件，实际包名和命令仍为 `word2jats`。保留六部分内容和评价协议，说明模型配置与版本、结构参考、评价单位和 Token 口径。与收尾前备份比较，全部实验表格的数据保持一致，差异仅为文件格式表头和模型文档链接。

Word/PDF 同步为 15 页，逐页查看通过。正文两端对齐、黑色，中文为宋体类 Noto Serif CJK SC，西文为 Times New Roman；PDF 嵌入字体。目录改为单列两级，并填入实际页码。表格宽度 9581 twip（约 16.9 cm），位于 17 cm 正文范围内；数值短列居中、叙述列左对齐，跨页表头重复。清理不必要的硬分页与连排，消除末尾空页。

Word 有 43 个有效书签；PDF 有 43 个命名目的地和 82 个实际链接注释，内部链接的目标为有效页对象。目录填页码后再次导出并回算，页码映射未变化。两张界面图为真实网页的 2880 × 1800 截图，没有修改页面内容。

版式证据：`reports/finals-closeout-20260914/说明书最终页面/`；目录映射为同目录的 `目录页码-最终.json`。链接补全由 `scripts.finalize_proposal_pdf` 完成；今后如重新导出 PDF，必须重新执行该步骤并校验页码。

## 功能与视觉覆盖

| 项目 | 结果 | 证据（均位于 reports/finals-closeout-20260914/） |
|---|---|---|
| 全量回归 | 645 通过，5 项既有条件跳过 | 全量回归-最终.txt |
| 已批准功能矩阵 | 64/64 | Web全量复验/验收清单.json；P11、E11、E18 由单元/API 与真实重启补充 |
| 独立视觉矩阵 | 原矩阵 58/58，另增 2 种用量状态 | Web全量复验/；其中 4 张用量截图由用量状态最终截图/中的最终截图替代 |
| 新用量状态 | 6/6，均已实际查看 | 用量与成果最终核验.json、用量状态最终截图/ |
| 定稿包真实界面 | 两配置各 4 张，均已实际查看 | 定稿包验收/Qwen真实转换/、DeepSeek真实转换/ |
| 浏览器异常 | 成功流程均无未处理 JavaScript 错误 | 各浏览器验收 JSON |

100% 指已列明需求和界面状态，不等于全部代码行或无穷输入组合。5 项跳过分别为：1 项等价变换不适用、1 项缺少既有评估输出、3 项缺少旧集成测试专用缓存；两模型的实际联网转换另行完整验证。全量回归后最后一次改动仅为“缺失原始计量”状态的一句提示和验收脚本的下载超时，该状态及两模型实际流程已单独复验。

原浏览器脚本生成的清单仍写有“待逐张人工查看”；那是生成阶段的状态。本记录确认最终已查看 54 张未变界面、4 张更新用量界面和 2 张新增用量界面，合计 60 张，并逐页查看 15 页 PDF。测试图只作内部证据，不混入说明书。

## 独立安装与实际转换

最终 ZIP 解压到 `/tmp/word2jats-final-package-5e14x3le/JiangLab`，在其 `可运行原型/.venv` 创建全新环境，按 README 安装运行依赖与项目。`pip check`、CLI/Web 入口、DTD 和预览资源加载均通过。实际运行不依赖开发仓库中的样例、评测脚本或输出；验收程序从外部上传测试稿件，密钥仅注入测试进程，不写入提交包。

定稿包在空的应用缓存上完成以下真实转换，均通过上传、预览、原稿下载字节核对、字段修改、下载当前 XML、修改不增加用量、恢复原 XML 和刷新恢复：

| 配置 | 任务 | 实际调用 | 输入 Token | 输出 Token | 缓存命中 | 缓存未命中 |
|---|---|---:|---:|---:|---:|---:|
| Qwen | 7bcc9c89edec401a | 32 | 532,212 | 35,305 | 99,584 | 432,628 |
| DeepSeek | 8d647aa53d37433c | 33 | 476,006 | 39,631 | 469,245 | 6,761 |

以上是交付验收，不替换说明书中的冻结实验。记录见 `定稿包验收/独立包安装验收.json` 和两种配置各自的 `验收.json`。

此前同轮独立包还完成了两配置的首次转换与同稿复用对照：Qwen 原始总量 567,761 Token，DeepSeek 519,008 Token；复用后原始总量不变，新增均为 0，XML SHA-256 不变。逐条核对磁盘缓存原始 `usage` 与请求 ID，并检查成果包内 XML、图片引用、原稿媒体字节和用量记录一致。证据为 `用量与成果最终核验.json`。CLI 在独立安装环境真实运行 S03/DeepSeek，退出码 0，DTD 通过；该次后仅调整 CLI 显示术语，命令参数及转换行为不变。

## 线上兼容与成果命名

复用回答现在连同原始 API 计量一起保存；结果用量与本次新增量分开返回。重复使用同一物理响应不会重复累计。旧任务没有原始计量时不把新增零冒充结果用量，也不按文字估算或按样例借用其他任务数据。

对线上已有任务，仅在禁用网络的回放中确认请求缓存存在、缓存早于原转换完成、请求计量完整、复用次数一致且原始 XML 逐字节相同，才恢复原计量。11 份任务满足条件，原快照已备份。确认活动任务为 0 后切换服务；12 份已完成任务均可打开，XML 与原新增计量保持不变。证据为 `历史用量恢复/`、`线上切换前.json`、`线上切换后.json`。

公开域名全流程复验任务为 `95170b7289ca4998`，Qwen，同稿复用 32 次、新增为零，原始总量 567,473 Token。下载、修改与恢复验证通过，见 `线上最终Qwen复验/验收.json`。首次公开域名检查在原稿文件下载中超过验收脚本默认 30 秒；返回已为 200，后按文件传输场景延长验收等待并通过，没有修改转换逻辑。

成果包按稿件标识命名，例如 `Word2JATS-CEOG50327.zip`；主体为 `CEOG50327.xml` 与 `figures.zip`，附简短文件说明、检查摘要、转换用量及必要修改记录。不再以“初始文件”命名交付，不打入调试文件或未被 XML 引用的旧资源。媒体无法完整交付时阻止生成残缺包。

## 恢复与后续维护

修改前 Git 标签为 `pre-finals-closeout-20260914`，文档和生成脚本备份为 `提交前备份-20260914-fcioIi/`。所有旧提交包保留于 `dist/archive/`。原始任务快照保留于 `reports/finals-closeout-20260914/历史用量恢复/`。不得用 `git reset --hard` 或覆盖当前任务目录回退；需在独立工作树恢复代码，并保留任务及修订文件。

交付代码以 `finals-submit-20260914` 标签和包内清单为准。后续仅补充内部验收记录的 Git 提交不会改变包内容，不应为追赶内部记录的提交号而无意义地重打包。用户的 AGENTS.md 改动未提交、未还原。临时验收服务结束后停止，正式 `word2jats-web` 服务继续运行。
