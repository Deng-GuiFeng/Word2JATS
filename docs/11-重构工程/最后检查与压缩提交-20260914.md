# 提交前最后检查与压缩

## 本轮要求与固定顺序

用户在重新检查目录后授权生成 ZIP，取代此前“不生成 ZIP”的要求。检查对象是 `dist/JiangLab/`，根目录只保留 `技术方案说明书.pdf` 和 `可运行原型/`；不恢复答辩、邮件、样例数据、历史结果或内部验收材料。压缩文件以队名命名为 `dist/JiangLab.zip`。

面向了解领域和比赛、但不了解本项目的评委，核对说明书展示的能力与当前原型一致、运行指引足以独立使用。以最新决赛通知的提交内容和评分维度为准，并检查项目介绍中列出的功能与运行说明要求。保留 `.env` 中获准供评审使用的两套测试密钥，不输出密钥，不修改用户的 AGENTS.md。

1. 重新阅读原始需求、最新通知、项目介绍及当前验收记录，区分历史状态和现状。
2. 检查 PDF 全文、实验数据对应关系、术语、图表版式、页面与链接。
3. 逐文件比对原型、工作区源码、两机全量验收指纹；检查配置来源、运行资源、文件边界及 README。
4. 复核既有本机/A800/容器的完整功能和视觉证据，在独立临时副本复验必要运行入口。不重新创建已清理的 A800 环境，不修改正式服务。
5. 没有提交阻断问题后，只压缩当前目录；校验 ZIP 成员、CRC、路径安全、UTF-8 文件名和解压后的逐文件哈希，确认隐藏配置随包且无额外文件。
6. 清理本轮临时环境，保留脱敏检查结果于提交目录外，报告实际检查结果和最终 ZIP。

本轮开始代码版本为 `0f3bb50`。此前本机和 A800 的功能矩阵均为 64/64、视觉状态均为 61/61；代码语句覆盖率约为 83.5%，不能把前两项覆盖范围称为所有代码或任意输入的绝对正确性。

## 结果

### 已完成的核查

- 通知要求提交完整作品（技术方案说明书、可运行原型等）并按队名命名；本目录两项材料与用户指定范围一致。项目介绍中的内容识别模块、原创实现、运行说明，以及最新通知的创新性、技术深度与工程质量、方案完整性，在说明书的方法、工程、原型和实验各节中均有对应内容。现场答辩准备不在本轮范围。
- 原型全部 166 个文件与两机验收文件清单比对，仅两份已获确认的 README 修订不同；源码、资源、配置与受测版本相同，全部运行文件与工作区最新版一致。没有样例数据、缓存、历史产物、测试和内部文档。
- 初赛 `.env` 除用户指定补入的 DeepSeek 密钥外逐字节不变。两套密钥只出现在获准提交的配置文件中；三个实际模型均在新环境调用成功，记录不含密钥。
- PDF 共 15 页，嵌入字体完整，455 个实验表格单元及其行归属均与冻结实验对应。费用独立重算为 Qwen 11.83912544 元、DeepSeek 8.41261640 元，与四位小数呈现一致。直接读取 DeepSeek 官方价格页确认高峰费率 2、0.04、8 元/百万 Token；搜索缓存的旧型号/费率未用于改写当前数据。
- 15 页均重新渲染并实际查看；单列两级目录、表格宽度、跨页表头、图示与截图、页面衔接正常。PDF 82 个链接注释和 43 个跳转目标有效，目录映射与实际页码一致。6 个外部技术文档链接均返回 200；在线原型在浏览器 User-Agent 请求下返回 200，系统级正式服务仍为 active，未进行启停或更改。
- 新建 `/tmp/word2jats-prezip-316nBu/prototype`，用全新 Python 3.12 环境按 README 安装依赖和项目；`pip check`、CLI/Web 帮助、网页实际启动、静态资源、21 项期刊配置及配置路径拒绝访问均通过。额外测试依赖和测试数据仅在临时环境，不进入提交目录。
- 在该副本上执行全部 650 条回归用例：645 通过、5 条件跳过，142.81 秒；跳过理由与此前相同。实际导入的核心与 Web 代码来自原型副本，测试和数据在独立 checker 目录。语句覆盖率 83.5088%，不称为 100% 代码覆盖。既有两机 64 项功能与 61 项视觉状态的证据已核对，与当前运行代码对应。全部前端 JavaScript 另通过语法检查。

证据位于 `reports/finals-lastcheck-20260914/`，只读目录与 ZIP 校验工具为 `scripts/verify_submission_directory.py`。

### PDF 导出细节

正文逐段对回 Word 后发现第 5 页“数学对象进入公式结构。”与第 9 页“用户查看。”各有一处行末句号在 PDF 导出中未显示。Word 与 Markdown 原文均完整，除此之外没有正文文字缺失。已向用户确认是否仅修复该导出细节或保持当前 PDF 后打包；等待答复期间不改提交文件。

在独立 LibreOffice 配置目录中用未修改的 Word 重新导出，复现了相同的两处缺失，确认问题发生在 PDF 导出，而非源稿遗漏或链接补全步骤。未修改提交 Word/PDF。

ZIP 尚未生成，等待用户确认。临时网页服务已停止；本轮 `/tmp/word2jats-prezip-316nBu` 内的测试副本、环境、数据、配置和诊断导出文件已清理，18637 端口关闭。正式服务未操作，提交目录保持原样。
