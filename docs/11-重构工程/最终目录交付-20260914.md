# 最终目录交付与异机验收

## 本轮边界（Compact 后重读）

以用户最后指令为准：不生成提交 ZIP；`dist/JiangLab/` 仅保留技术方案说明书 PDF 和可运行原型。删除指定答辩、邮件材料。说明书内容不再修改，原型须与最新运行源码一致。初赛 ZIP 内 `.env` 原样复制，仅将用户随后提供的 DeepSeek Key 补入原空字段；不使用开发密钥替换，不把密钥写入 Git 或报告。

A800 使用独立目录、独立虚拟环境和回环端口；不改系统 Python，不操作现有服务。测试完取回脱敏证据，停止本轮进程并移除本轮所有远端文件、环境、凭据和缓存。用户的 AGENTS.md 改动不动。

## 固定顺序

1. 保留 Git 基线 `pre-finals-directory-20260914`，指定删除文件可恢复归档。
2. 整理目录、补配置、同步简明运行说明；逐文件比对提交原型与工作区运行代码。所有额外测试材料均在提交目录外。
3. 本机全新副本安装与全量回归；A800 独立复制同一原型，按说明安装，校验源码、依赖和入口。
4. 使用提交配置进行两模型真实 Web 转换；检查上传、预览、原稿、用量、编辑保存、XML/图片下载、复用和重启恢复；完整执行既定 Web 功能与视觉矩阵，并检查异常路径。CLI 和容器入口按所含能力验收。
5. 将每个测试结果和实际覆盖范围写入内部记录。覆盖按明确需求、模块、接口和状态逐项核对，不把数量或静态导入冒充完整运行，不作无条件绝对正确承诺。
6. 清理 A800 本轮痕迹，最终确认提交目录无测试产物、ZIP 不存在、PDF 未改变、运行代码最新、两种密钥配置正确且未泄露。

## 执行位置

- 本机隔离目录：`/tmp/word2jats-directory-local-EsnxUV`。
- A800 隔离目录：`/tmp/word2jats-finals-isolated-UFWIxn`。
- 内部证据：`reports/finals-directory-20260914/`。
- A800 系统 Python 3.8 不符合项目要求；使用已有 `/opt/anaconda3/bin/python`（3.10.9）创建全新虚拟环境，正好验证声明的 Python 3.10 下限。

## 验收结果

提交目录已按最新要求整理完成。根目录仅 `技术方案说明书.pdf` 和 `可运行原型/`，无 ZIP、校验旁文件、Word 版、根 README 或清单。说明书 PDF 的 SHA-256 仍为 `7474f3ad0b56899f22338549430f60eeee50634daf5eafffeb69a63dabdd684f`，没有改动内容或排版。

原型 166 个文件逐文件核验，提交目录、本机隔离副本与 A800 隔离副本指纹一致；核心源码与运行资源 135 个、Web 代码与资源 23 个均与工作区最新版本逐字节相同。配置来自初赛 ZIP，只补入指定 DeepSeek 密钥；其他配置保持不变。README 已明确预置测试配置及使用方式。密钥未进入 Git、报告或下载成果。

### 安装和运行

本机使用全新 Python 3.12 虚拟环境，A800 使用已有 Python 3.10.9 解释器创建全新虚拟环境，均按原型 README 安装运行依赖、安装项目、执行 `pip check`。A800 系统 Python 3.8.10 和现有环境没有修改。测试代码、数据与参考结果单独置于原型外部，回归测试导入的运行代码来自提交副本；真实 Web/CLI 启动不依赖测试目录。

两机均用提交 `.env` 完成 Qwen、DeepSeek 真实 Web 转换及同稿复用。以下为本轮独立验收的首次转换，不改写说明书中的冻结实验：

| 环境 | 配置 | 调用数 | 输入 Token | 输出 Token | 输入缓存命中 | 输入缓存未命中 |
|---|---|---:|---:|---:|---:|---:|
| 本机 | Qwen | 31 | 511,236 | 33,659 | 10,240 | 500,996 |
| 本机 | DeepSeek | 32 | 474,631 | 35,052 | 13,568 | 461,063 |
| A800 | Qwen | 32 | 532,192 | 35,808 | 465,920 | 66,272 |
| A800 | DeepSeek | 34 | 495,693 | 37,130 | 488,701 | 6,992 |

四组首次转换均 DTD 通过。实际上传、预览图片、原稿逐字节下载、编辑保存、下载当前 XML、修改不新增用量、恢复原版、刷新恢复全部通过。四组复用均新增调用与新增 Token 为零，原始用量和 XML 哈希与对应首次转换一致。逐调用核对缓存原始 usage 和请求 ID；核对图片引用、图片原字节及成果包内容一致。

两机另执行了真实 DeepSeek CLI 转换，均退出 0、DTD 通过。两机各 4 份任务经实际停止服务、重新启动，人工修改、XML、用量和下载均保留；重启后恢复原版也通过。容器从提交目录实际构建，依赖和运行资源检查通过，镜像不含 `.env`；通过运行时配置注入完成 DeepSeek 真实浏览器转换、编辑和下载。

### 功能与视觉

| 检查 | 本机 | A800 |
|---|---|---|
| 全量回归 | 645 通过、5 条件跳过 | 644 通过、6 条件跳过 |
| 既定功能矩阵 | 64/64 | 64/64 |
| 既定视觉矩阵 | 58/58 | 58/58 |
| 新增视觉状态 | 3/3 | 3/3 |
| 两模型首次及复用实际流程 | 4/4 | 4/4 |
| 真实服务重启后的任务恢复 | 4/4 | 4/4 |

功能矩阵中 61 项由完整浏览器脚本完成，P11、E11、E18 由进程重启、既有字段无损单元/API 测试及重启修订保留验证补齐。116 张最终矩阵截图按文件 SHA-256 形成 66 组，逐组实际查看；完全相同的图片对应两机各自留证。另逐张查看两机共 6 张补充状态截图、16 张首次实际转换截图和 4 张容器实际转换截图。新增状态为无法解码图片的下载入口、缺失历史用量及窄屏复用用量。

5 项共同条件跳过为：1 项等价变换不适用于该稿、1 项依赖未随测试带入的旧评估输出、3 项旧缓存式集成测试在离线回归环境未配置密钥。真实模型转换已按上述独立联网流程验证，但不把这些旧用例写成通过。A800 多 1 项跳过是可选 WMF 系统解码器不可用。语句覆盖率另计：本机 83.52%、A800 83.51%，不是 100%；本文件的 100% 指已列明功能和视觉状态覆盖，不表示所有代码行、所有操作系统或任意稿件都绝对无误。

异机测试发现并修复了无 WMF 解码器时的预览破图：`webapp/static/preview.js` 现在将无法显示的本地图片替换为明确的原图下载入口。有解码器时仍正常预览，原始图片和 XML 不改变。该修复已同步至工作区、提交副本及两机测试副本；随后两机完整 Web 矩阵与模拟解码失败测试全部通过。此为本轮唯一运行代码修改，没有改识别提示、模型请求或核心转换语义。

所有证据均在 `reports/finals-directory-20260914/`：两机安装、全量回归、原型审计、代码覆盖 JSON、各真实流程目录、全状态最终目录、补充状态、重启验收及 `视觉逐项复查.json`。测试记录不进入提交材料。

### 清理与恢复

A800 本轮服务与测试进程已停止；隔离目录 `/tmp/word2jats-finals-isolated-UFWIxn`（约 701 MiB，含虚拟环境、依赖缓存、原型、测试数据和凭据）及本轮 pytest 临时目录 `/tmp/pytest-of-denggf/pytest-0`（约 223 MiB）已删除，`pytest-current` 链接及空父目录一并移除。必要脱敏证据已先取回本机。没有安装系统软件或占用 GPU，没有修改现有服务和系统 Python。

指定删除的 4 份答辩/邮件文件已移至 `docs/11-重构工程/目录整理备份-20260914/`，旧 ZIP、SHA 文件及根目录多余文件保留在 `dist/archive/目录整理-20260914/`，均可恢复，不属于当前提交目录。此前的 `提交前最终验收-20260914.md` 记录的是上一版 ZIP 交付；当前以本文件和可检查的目录为准，勿再运行旧打包脚本生成 ZIP 或恢复旧目录结构。

清理后复查确认两个测试端口均关闭、隔离目录和 pytest 临时目录不存在、无相关运行进程，见 `A800清理核验.json`。本机临时服务、测试容器和带凭据的隔离目录也已清理；正式 `word2jats-web` 服务保持运行。

状态：全部完成。最终只读复核再次确认根目录只有规定两项、原型全部 166 个文件与已验收副本一致、源码最新、初赛 `.env` 除指定新增密钥外逐字节不变、指定删除项和 ZIP 均不存在。后续由用户自行压缩提交目录。
