# word2jats 运行依赖（已在 Python 3.12 + 下列版本实测通过）
# 安装： pip install -r requirements.txt
python-docx>=1.1,<2      # docx 对象模型解析（段落/run/表/样式）
lxml>=5.2                # XML 构建、XSLT(OMML→MathML)、DTD 校验
Pillow>=10.0             # 图片格式转换与外部化（TIF/PNG→JPG）
PyYAML>=6.0              # 期刊查表等配置
requests>=2.31           # 可选：CrossRef 元数据富化
openai>=1.30             # 可选：LLM 语义增强（DeepSeek/DashScope，OpenAI 兼容）
python-dotenv>=1.0       # 可选：加载 .env 中的 API Key
