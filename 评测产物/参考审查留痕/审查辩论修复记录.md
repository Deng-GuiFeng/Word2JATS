# 10 例 结构参考.xml 全面审查 · 独立辩论 · 修复 记录

判断依据只有两条:**源 docx(内容唯一权威)+ 本项目规则**(设计文档/映射规格);**绝不拿"上线版本"当内容标准**。

## 1. 方法
- **审查团**:每例 2 个独立 opus 审查员逐区 100% 覆盖对 docx 查(API 抖动分两轮补跑);疑点存 `疑点汇总_第一轮.json` 及各 `<key>/auditor_*.md`。
- **主进程确定性交叉核查**(不靠 agent,可复现):跨 10 例查 xref 区间漏链、参考↔docx 词多重集(查 C 误入)、标题忠实、id 命名(对齐转换器/house-style)。
- **辩论团**:每条疑点 3 名独立法官(含专替参考辩护的反方),回 docx 定论;带自动重试,30 法官全成功、0 报错。

## 2. 辩论定论(10 条,均 3/3 一致)
**真错(已/待修)**
- `xref-ranges`(03/05/S01/S05):引用区间只链两端点、中间号漏链(house-style 与转换器均展开为逐号)。→ **已修**。
- `02-tables-C`(02):8 张表 docx 里是位图(w:tbl=0),参考的单元格文字 docx 全无、逐字来自上线版本 OCR(C 档),违反只加结构不改内容。→ 改按图处理,**已修+复验**(8 表只留 label+caption+graphic,位图与 docx 内嵌图 md5 逐字节一致,OCR 词 EROA/LVEF/Nitinol 清零;并复位 Table 1 题注 docx 原有的加粗)。
- `01-05-idnaming`(01/05):图/表/sec id 偏离 house-style(F1/T1/sec1 应为 F001/T001/S1),graphic/表脚注缺 id。→ **已修**。
- `02-caption-space`(02):Table 3 题注多一空格(China' s 应 China's)。→ 随 02 表格一并修。
- `05-RT-thead`(05):Figure 1-4 下 numbers-at-risk 表(RT1-RT4)的表头空 `<th/>` 放在了**末列**,而 docx 表头空格在**首列**(前导 tab=年龄类桩头),导致"1 month/60 months…"整体相对数据左移一列(274 落到 60 months 下)。→ 空 th 移到首列,**已修**(RT1-RT4 四表同改,只挪空单元格、不动任何数值,DTD 过)。

**不是错(参考本就对,不动)**
- `05-RT-spurious`:RT1-RT4 曾疑为"重复表/伪表(OCR自图片或编造)"。3 法官(含反方)一致**证伪**:docx 无 w:tbl,四块数据是"Figure N:"见出下方的 tab 文本,80 个数据格与 docx 逐字精确相等,"none"/脚注"elurgent"(docx 笔误)原样保留;四表两两不同、也不与 T001-T010 重复(RT=在险人数,T009=生存率%,T010=predictors)。→ 保留,不删。无 label/caption 正确(docx 无"Table N"见出,唯一"Figure N:"已归 fig),补 label 反而是编造。id RT1-RT4 可接受(无 label 套不进 T00N,且不被 xref 指向、DTD 合法)。
- `S02-title`:参考标题完整(`Introduction—<italic>Signs o' the Times</italic>`),未截断。**是评测 bug**(章节标题用 findtext 漏了 italic 子元素)→ **已修评测代码**。
- `abstract-structured`:docx 摘要含 Background/Methods 粗体标签,拆 4 sec 是正确结构化。
- `S04-hash`:孤立 # 单人无法成立共同贡献,equal_contrib=false 正确(此前已拍板)。
- `journal-amp`:CEOG 真实刊名即含 `&`,参考对(审查指令把 & 写成 and 才误报)。
- `04-orcid-aff`:ORCID/单位归属参考对,是转换器认错人。
- `pubid-doi`:publisher-id 用完整 DOI 是 house-style(上线版与转换器一致)。

## 3. 已落地修复(均过 DTD、悬空 xref=0)
- 评测:`scripts/eval/structure.py` 章节/back 标题改用全文 itertext(不再漏 italic 子元素)。
- 01/05:图/表/sec id 对齐 house-style(F001/T001/S{n}),补 graphic(F00N.g1)与表脚注(T00N-fnK)id,同步更新 xref rid。
- 03/05/S01/S05:xref 引用区间按 house-style 逐号展开;**回验漏链已全部清零**(03 补 b12/22/29/48/49,05 补 b21,S01 补 b14/18,S05 补 b2/16/19/25/40/45)。
- 02:8 张位图表改按图处理(table-wrap 内只 label+caption+graphic,指向 figures.zip 里 md5 与 docx 内嵌图逐字节一致的 table-01..08.jpg),清除 OCR 单元格与 InnerTable-Graphic;题注 China' s→China's;复位 Table 1 题注加粗(docx 原有)。
- 05:RT1-RT4 表头空 `<th/>` 从末列移到首列,消除表头与数据的一列错位(只挪空单元格,数值不动)。
- 收口复验:10 例 结构参考.xml 全部 **DTD PASS、悬空 xref=0、无重复 id**。

## 4. 结论
本轮"逐个独立全覆盖审查 → 每疑点 3 法官独立辩论 → 有 docx 证据才动"已走完:辩论确认的真错(评测取全文 bug、01/05 id、xref 区间、02 表格 C 档、02 题注空格、05 RT 表头错位)**全部修复并复验**;判为误报的 7 条(S02 标题、摘要拆 sec、S04 的 #、刊名 &、04 ORCID、publisher-id、05 RT 重复/伪表指控)**一律未动参考**。10 例 结构参考.xml 均过 DTD、悬空 xref 清零。
